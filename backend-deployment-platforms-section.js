(()=>{
  if(window.__backendDeploymentPlatformsAdded)return;
  window.__backendDeploymentPlatformsAdded=true;
  if(!Array.isArray(window.sections)||typeof window.T!=='function')return;

  const preview=(title,html)=>`<section style="font-family:system-ui;padding:16px;border:1px solid #334155;border-radius:12px;background:#07111f;color:#e5edf8"><strong style="display:block;margin-bottom:8px;color:#a78bfa">${title}</strong>${html}</section>`;
  const file=(path,detail,method='MANUAL',command='')=>({path,method,detail,command});
  const D=(topic,name,description,code,previewHtml,tip,meta={})=>T(topic,name,description,code,previewHtml,[],{
    kind:'Backend deployment',tip,
    guideTitle:'Dónde se hace cada modificación',
    codeLabel:'Configuración / comandos',
    filesToCreateTitle:'Archivos que crea el estudiante',
    filesToCreateStatus:'Versiona la configuración reproducible; guarda secretos únicamente como variables del proveedor o en un gestor de secretos.',
    ...meta
  });

  sections.push({
    title:'Backend · Despliegue · Railway vs Render vs Fly.io',
    navLabel:'Despliegue · Railway vs Render vs Fly.io',
    group:'Backend',primaryArea:'Backend',course:'Backend',areaOrder:35,
    description:'Compara tres formas modernas de desplegar backends. Prepara una API real para producción, configura puerto, health check, variables de entorno, base de datos, Docker y observabilidad, y entiende qué cambia entre Railway, Render y Fly.io.',
    quote:'“El despliegue empieza preparando bien la aplicación; la plataforma viene después.”',
    challenge:'Publica la misma API pequeña en una de las plataformas, conecta una base de datos, configura /health y documenta el proceso de rollback.',
    items:[
      D('preparación','1. Qué necesita un backend antes de desplegar','Una API desplegable debe escuchar el puerto que entrega el entorno, exponer un endpoint de salud, leer configuración desde variables de entorno, cerrar conexiones correctamente y tener un comando de inicio reproducible. Si eso no funciona localmente, cambiar de plataforma no lo arreglará.','// Node + Express\nconst port = Number(process.env.PORT || 3000);\n\napp.get("/health", (_req, res) => {\n  res.json({ ok: true });\n});\n\napp.listen(port, "0.0.0.0", () => {\n  console.log(`API listening on ${port}`);\n});\n\n// package.json\n{\n  "scripts": {\n    "start": "node src/server.js"\n  }\n}',preview('Checklist','<p style="margin:0">PORT · /health · start command · env vars · logs · conexión DB.</p>'),'No fijes un puerto de producción si el proveedor entrega PORT dinámicamente.',{
        guide:[['Modificar','src/server.js','Lee PORT y expone /health.'],['Modificar','package.json','Define el comando start.'],['Crear','.env.example','Documenta variables sin secretos.']],
        filesToCreate:[file('src/server.js','Punto de arranque del backend.'),file('.env.example','Plantilla sin secretos.'),file('package.json','Scripts de ejecución.')]
      }),
      D('Railway','2. Railway · flujo básico de despliegue','Railway permite conectar un repositorio y crear servicios con variables de entorno y recursos asociados. El flujo típico es importar el repo, seleccionar el servicio, definir el comando de inicio si no se detecta automáticamente, agregar variables y desplegar. La interfaz y los planes pueden cambiar, por eso debes entender los conceptos en vez de memorizar botones.','git push origin main\n\n# Variables en el proveedor\nNODE_ENV=production\nDATABASE_URL=...\n\n# Comando de inicio\nnpm start',preview('Railway','<p style="margin:0">Git push → build → service → env vars → dominio → logs.</p>'),'No subas DATABASE_URL ni tokens al repositorio; configúralos en el panel o mecanismo de secretos de la plataforma.',{
        guide:[['Revisar','Railway · Service settings','Configura build/start y variables del servicio.'],['Modificar','.env.example','Documenta las variables requeridas.']],filesToCreate:[file('.env.example','Contrato de configuración.')]
      }),
      D('Render','3. Render · Web Service y configuración','En Render un backend suele desplegarse como Web Service desde Git o Docker. Debes proporcionar build command, start command, variables de entorno y health check cuando corresponda. También puede trabajar con servicios administrados adicionales. Evita asumir que todas las opciones y precios son permanentes; valida el plan vigente antes de un proyecto real.','# ejemplo conceptual\nBuild command: npm ci\nStart command: npm start\nHealth check: /health\n\nVariables:\nNODE_ENV=production\nDATABASE_URL=...',preview('Render','<p style="margin:0">Repositorio → build → Web Service → health check → dominio HTTPS.</p>'),'Separa build de start: instalar/compilar no es lo mismo que ejecutar el proceso web.',{
        guide:[['Revisar','Render · Web Service settings','Define build, start, health y variables.'],['Modificar','src/server.js','Mantén /health rápido y sin trabajo pesado.']],filesToCreate:[file('src/server.js','Servidor y health check.')]
      }),
      D('Fly.io','4. Fly.io · despliegue con máquinas y regiones','Fly.io orienta el despliegue alrededor de aplicaciones y máquinas distribuidas. Su flujo suele apoyarse en CLI y configuración declarativa. Es útil cuando necesitas mayor control de imagen, regiones, red privada o colocación geográfica. Ese control implica entender mejor Docker, procesos y capacidad.','fly launch\nfly deploy\nfly status\nfly logs\n\n# fly.toml\napp = "mi-api"\n\n[http_service]\n  internal_port = 3000\n  force_https = true',preview('Fly.io','<p style="margin:0">Código/Docker → fly deploy → Machines → regiones → tráfico HTTPS.</p>'),'No distribuyas una aplicación entre regiones sin pensar dónde vive la base de datos y cuánto cuesta la latencia entre regiones.',{
        guide:[['Crear','fly.toml','Declara puerto y configuración de la app.'],['Ejecutar','Terminal · raíz del backend','Usa fly launch/deploy/logs.']],filesToCreate:[file('fly.toml','Configuración de Fly.io.','CLI','fly launch')]
      }),
      D('Docker','5. Docker como contrato portable','Un Dockerfile reduce diferencias entre proveedores porque define exactamente cómo construir y ejecutar la aplicación. Railway, Render y Fly.io pueden trabajar con contenedores en distintos flujos. Mantén imágenes pequeñas, usa una versión concreta de runtime y evita copiar secretos dentro de la imagen.','FROM node:22-alpine\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci --omit=dev\nCOPY . .\nENV NODE_ENV=production\nCMD ["npm", "start"]\n\n# .dockerignore\nnode_modules\n.env\n.git',preview('Imagen reproducible','<p style="margin:0">Dockerfile + código → imagen → mismo proceso en local y plataforma.</p>'),'Un contenedor reproducible no reemplaza variables, backups, observabilidad ni estrategia de migraciones.',{
        guide:[['Crear','Dockerfile','Describe build y proceso de producción.'],['Crear','.dockerignore','Excluye secretos y archivos innecesarios.']],filesToCreate:[file('Dockerfile','Imagen del backend.'),file('.dockerignore','Exclusiones del contexto Docker.')]
      }),
      D('database','6. Base de datos y persistencia','No guardes datos importantes en el filesystem efímero del proceso web salvo que la plataforma documente almacenamiento persistente y lo configures explícitamente. Para aplicaciones comunes usa PostgreSQL, Redis u otro servicio persistente y conecta mediante DATABASE_URL. Ejecuta migraciones de forma controlada durante el despliegue.','DATABASE_URL=postgresql://...\n\n# ejemplo de release/migración\nnpm run migrate\n\n# después\nnpm start',preview('Persistencia','<p style="margin:0">Servicio web ↔ DATABASE_URL ↔ base persistente. El disco del proceso no es tu base de datos.</p>'),'Haz backups y prueba restauración; tener una base administrada no sustituye una política de recuperación.',{
        guide:[['Modificar','.env.example','Declara DATABASE_URL sin valor real.'],['Crear','scripts/migrate.sh','Opcional: agrupa el comando de migración.']],filesToCreate:[file('.env.example','Variables requeridas.'),file('scripts/migrate.sh','Migraciones reproducibles.')]
      }),
      D('secrets','7. Variables de entorno y secretos','Las variables públicas de configuración y los secretos no deben tratarse igual. El repositorio puede contener .env.example con nombres, pero no claves reales. Configura secretos desde la plataforma y rota credenciales cuando sea necesario.','// config.js\nconst required = ["DATABASE_URL", "JWT_SECRET"];\nfor (const key of required) {\n  if (!process.env[key]) {\n    throw new Error(`Missing ${key}`);\n  }\n}\n\n// .gitignore\n.env\n.env.*\n!.env.example',preview('Regla','<p style="margin:0">Git: nombres y documentación · plataforma: valores secretos.</p>'),'Falla rápido al arrancar si falta una variable crítica; es mejor que fallar durante una petición real.',{
        guide:[['Crear','src/config.js','Valida configuración al arrancar.'],['Modificar','.gitignore','Ignora archivos de secretos.'],['Crear','.env.example','Lista claves necesarias.']],filesToCreate:[file('src/config.js','Validación de configuración.'),file('.env.example','Plantilla.'),file('.gitignore','Protección de secretos.')]
      }),
      D('observability','8. Logs, health checks y rollback','Un despliegue no termina cuando aparece una URL. Revisa logs, health checks, consumo de memoria/CPU, latencia y errores. Conserva la posibilidad de volver a una versión conocida si el release falla. Evita logs con contraseñas, tokens o datos personales sensibles.','app.get("/health", (_req, res) => {\n  res.status(200).json({ status: "ok" });\n});\n\nprocess.on("unhandledRejection", error => {\n  console.error("Unhandled rejection", error);\n});',preview('Operación','<p style="margin:0">Deploy → health → logs → métricas → alertas → rollback si falla.</p>'),'Un health check debe responder rápido y reflejar si el proceso puede atender tráfico.',{
        guide:[['Modificar','src/server.js','Expone /health y errores de proceso.'],['Crear','docs/runbook.md','Documenta cómo revisar logs y revertir.']],filesToCreate:[file('docs/runbook.md','Guía de operación y rollback.')]
      }),
      D('comparison','9. Cómo elegir Railway, Render o Fly.io','Railway suele priorizar una experiencia rápida orientada a servicios y recursos conectados; Render ofrece un flujo PaaS claro para web services, jobs y despliegues desde Git/Docker; Fly.io ofrece más control sobre máquinas, imagen y distribución geográfica. No elijas por un ranking fijo: compara región, base de datos, escalado, observabilidad, red, experiencia del equipo y costo actual.','Criterios:\n- tiempo de puesta en marcha\n- soporte Docker\n- regiones necesarias\n- base de datos y backups\n- red privada\n- jobs/workers\n- logs y métricas\n- rollback\n- costo actual\n- experiencia del equipo',preview('Decisión','<p style="margin:0">Proyecto pequeño → simplicidad puede ganar. Baja latencia global/control → más infraestructura puede ser justificable.</p>'),'Los planes cambian: antes de producción revisa siempre documentación, límites y precios oficiales vigentes.',{
        guide:[['Crear','docs/deployment-decision.md','Compara requisitos reales y registra por qué eliges una plataforma.']],filesToCreate:[file('docs/deployment-decision.md','Matriz de decisión.')]
      }),
      D('project','10. Proyecto · desplegar una API de perfil','Cierra el bloque desplegando una API con GET /health y GET /profile. La aplicación debe usar PORT, leer variables, conectarse a una base externa y tener instrucciones reproducibles. Registra la URL, un ejemplo curl y el procedimiento de rollback.','backend-deploy/\n├── src/\n│   ├── server.js\n│   └── config.js\n├── Dockerfile\n├── .dockerignore\n├── .env.example\n├── package.json\n└── docs/\n    └── runbook.md\n\ncurl https://TU-DOMINIO/health',preview('Resultado','<p style="margin:0"><b>GET /health</b> → 200 { "status": "ok" }</p>'),'Primero despliega sin base de datos, luego conecta persistencia y por último prueba un rollback.',{
        guide:[['Crear','backend-deploy/src/server.js','API y health.'],['Crear','backend-deploy/src/config.js','Variables.'],['Crear','backend-deploy/Dockerfile','Imagen.'],['Crear','backend-deploy/docs/runbook.md','Operación y rollback.']],
        filesToCreate:[file('backend-deploy/src/server.js','Servidor.'),file('backend-deploy/src/config.js','Configuración.'),file('backend-deploy/Dockerfile','Contenedor.'),file('backend-deploy/.dockerignore','Exclusiones.'),file('backend-deploy/.env.example','Variables.'),file('backend-deploy/docs/runbook.md','Operación.')]
      })
    ]
  });
})();
