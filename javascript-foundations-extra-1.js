(()=>{
  if(window.__jsFoundationsExtra1)return; window.__jsFoundationsExtra1=true;
  if(!Array.isArray(window.sections)||typeof window.T!=='function')return;
  const L=(...x)=>x.join('\n');
  const F=(path,detail)=>({path,method:'MANUAL',detail});
  const R=(title,text)=>`<section style="font-family:system-ui;padding:16px;border:1px solid rgba(250,204,21,.38);border-radius:12px;background:#0b1220;color:#e5edf8"><strong style="display:block;color:#fde047;margin-bottom:8px">${title}</strong><div style="line-height:1.6">${text}</div></section>`;
  const J=(topic,name,desc,code,out,meta={})=>T(topic,name,desc,code,out,[],{kind:'JavaScript',tip:meta.tip||'Predice el resultado antes de ejecutar y cambia al menos un valor para comprobar la regla.',guide:meta.guide||[['Crear o modificar',meta.path||'javascript/fundamentos/app.js','Escribe aquí el ejemplo.'],['Ejecutar','Navegador · DevTools','Ejecuta y compara con el resultado esperado.']],guideTitle:'Dónde practicar y ejecutar',codeLabel:'Código JavaScript',filesToCreate:meta.files||[F(meta.path||'javascript/fundamentos/app.js','Archivo de práctica de esta lección.')],filesToCreateTitle:'Archivos que se crean en esta lección',exerciseTitle:'Ejercicio para ti',exerciseTasks:meta.tasks||['Ejecuta el ejemplo.','Cambia los datos por otros de práctica.','Explica en una frase por qué se obtiene ese resultado.'],exerciseExtra:meta.extra||'Intenta responder primero sin ejecutar; luego comprueba tu respuesta.'});
  const A=(title,areaOrder,desc,challenge,items)=>sections.push({title,navLabel:title.replace(/^JavaScript\s*·\s*/,''),description:desc,quote:'“Entender tipos y valores evita muchos errores antes de escribir aplicaciones grandes.”',challenge,group:'JavaScript',primaryArea:'JavaScript',course:'JavaScript',areaOrder,items});

  A('JavaScript · 1A. Sintaxis, entrada y salida',15,'Completa los primeros pasos con las formas de cargar JavaScript, las reglas de sintaxis y las maneras principales de recibir y mostrar información.','Crea una página que cargue app.js con defer, lea un nombre desde un input y muestre el saludo en el DOM y en la consola.',[
    J('script','1. Inline, interno y externo','JavaScript puede escribirse inline, dentro de una etiqueta script o en un archivo externo. Para proyectos reales conviene separar el código en archivos .js. El atributo defer permite descargar el archivo sin bloquear el análisis del HTML y ejecutarlo cuando el documento ya fue procesado.',L('<!-- Inline: solo demos pequeñas -->','<button onclick="console.log(\'Hola\')">Saludar</button>','','<!-- Interno -->','<script>',"  console.log('JavaScript interno');",'<\/script>','','<!-- Externo: recomendado -->','<script src="app.js" defer><\/script>'),R('Comparación','Inline mezcla estructura y comportamiento · interno sirve para demos · externo facilita mantenimiento, caché y reutilización.'),{path:'javascript/carga/index.html',files:[F('javascript/carga/index.html','Compara las tres formas.'),F('javascript/carga/app.js','JavaScript externo recomendado.')],guide:[['Crear','javascript/carga/index.html','Escribe el HTML y enlaza app.js con defer.'],['Crear','javascript/carga/app.js','Coloca aquí el comportamiento.'],['Abrir','Navegador · DevTools','Comprueba que no existan errores.']]}),
    J('syntax','2. Reglas básicas de sintaxis','JavaScript distingue mayúsculas y minúsculas, usa llaves para bloques y tiene reglas para identificadores. El punto y coma suele ser opcional por ASI, pero un estilo consistente evita ambigüedades. Un identificador no puede comenzar por número ni ser una palabra reservada.',L('const nombre = "Ana";','const Nombre = "Sara"; // variable distinta','','if (nombre.length > 0) {','  console.log(nombre);','}','','// Válidos: total, _total, $total, total2','// Inválidos: 2total, const, user-name'),R('Idea clave','nombre y Nombre son identificadores diferentes porque JavaScript es case-sensitive.'),{path:'javascript/fundamentos/sintaxis.js'}),
    J('output','3. Formas de mostrar resultados','console.log es ideal para depurar. alert muestra un diálogo bloqueante. textContent actualiza una parte de la interfaz de forma controlada. document.write existe por compatibilidad histórica, pero no se recomienda en aplicaciones modernas porque puede reemplazar el documento si se usa después de la carga.',L("console.log('Resultado en DevTools');","alert('Mensaje de demostración');",'',"document.querySelector('#salida').textContent = 'Resultado visible';",'','// document.write() es una técnica heredada; evita usarla.'),R('Cuándo usar cada una','console.log → depuración · alert → demo puntual · textContent → interfaz real · document.write → legado.'),{path:'javascript/entrada-salida/app.js',files:[F('javascript/entrada-salida/index.html','Contiene #salida y carga app.js.'),F('javascript/entrada-salida/app.js','Prueba las salidas.')]}),
    J('input','4. prompt, confirm e input HTML','prompt devuelve un string o null y confirm devuelve un boolean. Son útiles para ejemplos rápidos, pero en interfaces reales suele ser mejor usar elementos input y eventos porque no bloquean la página y permiten validación y diseño.',L("const nombre = prompt('Escribe tu nombre');","const continuar = confirm('¿Deseas continuar?');",'console.log(nombre, continuar);','',"const campo = document.querySelector('#nombre');","document.querySelector('#guardar').addEventListener('click', () => {","  console.log(campo.value.trim());",'});'),R('Tipos devueltos','prompt → string|null · confirm → boolean · input.value → string.'),{path:'javascript/entrada-salida/app.js'}),
    J('input value','5. Los valores de input llegan como texto','Aunque un input sea type="number", su propiedad value es una cadena. Convierte de forma explícita antes de calcular.',L("const edadTexto = document.querySelector('#edad').value;",'const edad = Number(edadTexto);','',"console.log(typeof edadTexto); // string","console.log(typeof edad);      // number"),R('Regla','La interfaz entrega texto; tu programa decide cuándo convertirlo.'),{path:'javascript/entrada-salida/app.js'})
  ]);

  A('JavaScript · 2A. Tipos especiales y memoria',25,'Profundiza en undefined, null, NaN, BigInt, Symbol, typeof, primitivos, referencias y el comportamiento real de const con objetos y arrays.','Explica con ejemplos la diferencia entre ausencia de valor, valor numérico inválido y referencia compartida.',[
    J('undefined null NaN','1. undefined vs null vs NaN','undefined suele indicar que todavía no se asignó un valor; null expresa ausencia intencional; NaN aparece cuando una operación numérica no puede producir un número válido. typeof NaN devuelve number por razones del modelo numérico de JavaScript.',L('let telefono;','const segundoNombre = null;',"const edad = Number('abc');",'','console.log(telefono);            // undefined','console.log(segundoNombre);       // null','console.log(edad);                // NaN','console.log(Number.isNaN(edad));  // true'),R('No son lo mismo','undefined → no asignado · null → ausencia intencional · NaN → cálculo numérico inválido.'),{path:'javascript/tipos/especiales.js'}),
    J('BigInt','2. BigInt y enteros enormes','Number representa enteros con precisión exacta solo hasta Number.MAX_SAFE_INTEGER. BigInt permite enteros mucho mayores y se escribe con n o BigInt(). No puedes mezclar directamente BigInt y Number en una operación aritmética.',L('console.log(Number.MAX_SAFE_INTEGER);','const grande = 9007199254740993n;',"const otro = BigInt('9007199254740995');",'console.log(grande + 2n);','console.log(typeof grande); // bigint'),R('Regla','BigInt conserva enteros enormes con precisión; 1n + 1 produce TypeError porque mezcla bigint y number.'),{path:'javascript/tipos/bigint.js'}),
    J('Symbol','3. Symbol · valores únicos','Cada llamada a Symbol crea un valor único, incluso con la misma descripción. Es útil como clave de propiedad cuando quieres evitar colisiones de nombres.',L("const idA = Symbol('id');","const idB = Symbol('id');",'console.log(idA === idB); // false','',"const usuario = { nombre: 'Ana', [idA]: 101 };",'console.log(usuario[idA]); // 101'),R('Resultado','Dos Symbols con la misma descripción siguen siendo valores diferentes.'),{path:'javascript/tipos/symbol.js'}),
    J('typeof','4. typeof y casos especiales','typeof funciona bien para tipos primitivos generales, pero tiene excepciones históricas: typeof null devuelve object y los arrays también devuelven object. Usa Array.isArray para reconocer arrays y comparación directa para null.',L('console.log(typeof "hola");      // string','console.log(typeof 42);          // number','console.log(typeof true);        // boolean','console.log(typeof undefined);   // undefined','console.log(typeof null);        // object (histórico)','console.log(typeof []);          // object','console.log(Array.isArray([]));  // true','console.log(typeof (() => {}));  // function'),R('Consejo','typeof + Array.isArray + comparación con null cubren los casos comunes.'),{path:'javascript/tipos/typeof.js'}),
    J('value reference','5. Primitivos vs referencias','Al asignar un primitivo a otra variable se copia su valor. Al asignar un objeto o array, ambas variables pueden apuntar al mismo objeto; una mutación hecha por una referencia se observa desde la otra.',L('let a = 10;','let b = a;','b = 20;','console.log(a); // 10','',"const usuarioA = { nombre: 'Ana' };",'const usuarioB = usuarioA;',"usuarioB.nombre = 'Sara';",'console.log(usuarioA.nombre); // Sara','', 'const copia = { ...usuarioA };'),R('Modelo mental','Primitivo → copia del valor · objeto/array → referencia al mismo objeto.'),{path:'javascript/tipos/referencias.js'}),
    J('const','6. const con objetos y arrays','const impide reasignar el identificador, pero no vuelve inmutable el objeto almacenado. Puedes cambiar propiedades o usar push; lo que no puedes hacer es asignar otra referencia a la misma variable.',L("const persona = { nombre: 'Ana' };","persona.nombre = 'Laura'; // permitido",'',"const cursos = ['HTML'];","cursos.push('CSS'); // permitido",'','// persona = {}; // TypeError','// cursos = [];  // TypeError'),R('Regla','const fija la referencia; no congela automáticamente el contenido.'),{path:'javascript/tipos/const-objetos.js'})
  ]);
})();