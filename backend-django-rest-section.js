(()=>{
  if(window.__backendDjangoRestAdded)return;
  window.__backendDjangoRestAdded=true;

  const terminal=text=>`<pre style="margin:0;padding:16px;border-radius:12px;background:#07111f;color:#86efac;font:13px/1.6 ui-monospace,SFMono-Regular,Consolas,monospace;white-space:pre-wrap;overflow:auto">${text}</pre>`;
  const panel=(title,body)=>`<section style="font-family:system-ui;border:1px solid #cbd5e1;border-radius:12px;overflow:hidden;background:white;color:#334155"><header style="padding:10px 14px;background:#0f172a;color:white;font-weight:700">${title}</header><div style="padding:14px;line-height:1.6">${body}</div></section>`;
  const flow=text=>`<div style="font-family:system-ui;padding:16px;border:1px solid #cbd5e1;border-radius:12px;background:#f8fafc;color:#334155;white-space:pre-line;text-align:center;font-weight:650">${text}</div>`;
  const D=(topic,name,description,code,preview,tip)=>T(topic,name,description,code,preview,[],{kind:'Backend Django REST',tip});
  const add=(title,description,quote,challenge,items)=>sections.push({title,description,quote,challenge,items});

  add('Backend APIs · FastAPI y Django REST','Ubica las dos rutas de aprendizaje de backend con Python. FastAPI ya está desarrollado en la biblioteca y esta sección añade Django REST Framework desde cero, ambos usando PostgreSQL.','“Dos frameworks distintos pueden resolver el mismo problema: recibir HTTP, validar reglas, guardar datos y responder JSON.”','Explica a un compañero qué cambia entre FastAPI y Django REST y qué parte cumple PostgreSQL.',[
    D('Comparación','¿Qué vas a aprender?','La biblioteca contiene una ruta completa de FastAPI y otra de Django REST Framework. En ambas construirás una API de productos conectada a PostgreSQL, pero cada framework organiza el proyecto de manera diferente. FastAPI es muy explícito y ligero; Django REST se apoya en el ecosistema Django, su ORM, migraciones, panel administrativo y convenciones.','FastAPI\n  rutas → Pydantic → SQLAlchemy → PostgreSQL\n\nDjango REST\n  urls → View/ViewSet → Serializer → Django ORM → PostgreSQL',flow('Frontend\n↓ HTTP + JSON\nAPI REST\n↓ reglas + ORM\nPostgreSQL'),'No intentes memorizar archivos. Aprende primero qué responsabilidad tiene cada pieza.'),
    D('Elección','¿Cuándo usar cada uno?','FastAPI resulta cómodo cuando quieres una API pequeña o mediana, tipada y muy explícita. Django REST es excelente cuando el sistema también necesita administración, autenticación, permisos, modelos relacionados y muchas funciones del ecosistema Django. Para aprender, ambos son valiosos porque muestran dos estilos de arquitectura.','FASTAPI\n+ rápido para APIs\n+ OpenAPI automático\n+ Pydantic\n\nDJANGO REST\n+ ORM integrado\n+ migraciones\n+ admin\n+ autenticación y permisos',panel('Idea clave','No existe un “mejor framework” universal. Elige según el proyecto, el equipo y las necesidades.'),'Para el curso, construye el mismo CRUD con ambos y compara el código.'),
    D('Base de datos','PostgreSQL es la fuente de verdad','Los datos no deben vivir solo en una lista de Python. PostgreSQL conserva la información aunque el servidor se reinicie, aplica restricciones y permite consultas, relaciones y transacciones. Tanto FastAPI como Django REST pueden usar la misma base conceptual, aunque sus ORMs sean diferentes.','Cliente → API → ORM → PostgreSQL\n                  ↑\n              tablas reales',panel('PostgreSQL','Guarda productos, usuarios, pedidos y demás información persistente. El navegador nunca debe conectarse directamente a la base de datos.'),'Las credenciales de PostgreSQL deben ir en variables de entorno, nunca escritas en el repositorio.')
  ]);

  add('Django REST · 1. Conceptos desde cero','Antes de crear archivos, comprende qué es Django, qué agrega Django REST Framework y qué significa construir una API.','“Si entiendes el recorrido de una petición, los archivos dejan de parecer piezas aisladas.”','Dibuja el flujo de un GET /api/productos/ desde el navegador hasta PostgreSQL.',[
    D('API','¿Qué es una API REST?','Una API es una interfaz para que programas se comuniquen. En una API REST normalmente exponemos recursos mediante URLs y usamos métodos HTTP para indicar la operación. El cuerpo suele viajar como JSON.','GET    /api/productos/       # listar\nPOST   /api/productos/       # crear\nGET    /api/productos/7/     # consultar uno\nPUT    /api/productos/7/     # reemplazar\nPATCH  /api/productos/7/     # modificar parcialmente\nDELETE /api/productos/7/     # eliminar',panel('Recurso: productos','La URL representa el recurso; el método HTTP representa la acción.'),'Empieza usando sustantivos en las URLs y evita rutas como /crearProducto.'),
    D('Django','¿Qué es Django?','Django es un framework web de Python. Incluye configuración, rutas, ORM, migraciones, seguridad, sesiones, autenticación y un panel administrativo. Un “proyecto” contiene la configuración general y una “app” agrupa una parte funcional del sistema.','Proyecto config/\n  settings.py  → configuración\n  urls.py      → rutas generales\n\nApp productos/\n  models.py    → datos\n  views.py     → lógica HTTP\n  admin.py     → panel admin',flow('Proyecto Django\n├─ configuración global\n└─ apps funcionales'),'Una app no es otro proyecto: es un módulo reutilizable dentro del proyecto.'),
    D('DRF','¿Qué agrega Django REST Framework?','Django REST Framework, o DRF, añade herramientas especializadas para APIs: serializers, APIView, ViewSets, routers, autenticación, permisos, paginación y una interfaz navegable para probar endpoints desde el navegador.','Django = web + ORM + migraciones + admin\nDRF    = serializers + vistas API + routers + permisos',panel('Resultado','Django administra el proyecto y la persistencia; DRF facilita exponer esa lógica como JSON.'),'Primero entiende una APIView manual; después usa ModelViewSet para reducir código repetitivo.'),
    D('Flujo','Recorrido de una petición','Cuando llega POST /api/productos/, Django resuelve la URL. La view recibe JSON, el serializer valida tipos y reglas, el ORM crea una fila en PostgreSQL y el serializer transforma el objeto guardado en JSON de respuesta.','POST /api/productos/\n        ↓\nconfig/urls.py\n        ↓\nproductos/urls.py o router\n        ↓\nviews.py\n        ↓\nserializers.py\n        ↓\nmodels.py / ORM\n        ↓\nPostgreSQL',flow('URL → View/ViewSet → Serializer → Model → PostgreSQL\n                              ↑\n                         JSON de salida'),'Cuando algo falla, identifica en qué paso del flujo ocurre antes de modificar código al azar.')
  ]);

  add('Django REST · 2. Preparar el equipo','Crea el proyecto desde una carpeta vacía, instala Python, el entorno virtual y todas las dependencias necesarias.','“Un entorno reproducible empieza por saber exactamente qué instalaste y dónde.”','Crea el entorno virtual y consigue que python manage.py runserver muestre la página inicial de Django.',[
    D('Carpeta','Crear la carpeta del proyecto','Abre una terminal en el lugar donde guardarás el trabajo. Crea una carpeta y entra en ella. Todos los comandos siguientes se ejecutan desde esa raíz.','mkdir api_django_rest\ncd api_django_rest',terminal('api_django_rest/'),'Usa nombres sin espacios para evitar problemas en terminales y herramientas.'),
    D('Python','Verificar Python y pip','Comprueba que Python está instalado. En Windows puede funcionar py; en Linux normalmente python3. La versión exacta puede variar, pero debe ser una versión soportada por tu Django instalado.','python --version\npython -m pip --version\n\n# Windows alternativo\npy --version',terminal('Python 3.x.x\npip xx.x from ...'),'Usar python -m pip ayuda a instalar paquetes en el mismo intérprete que estás ejecutando.'),
    D('venv','Crear y activar el entorno virtual','El entorno virtual aísla las dependencias del proyecto. Así no mezclas versiones de Django entre trabajos distintos. La carpeta .venv no debe subirse a Git.','python -m venv .venv\n\n# Windows PowerShell\n.\\.venv\\Scripts\\Activate.ps1\n\n# Windows CMD\n.venv\\Scripts\\activate\n\n# Linux / macOS\nsource .venv/bin/activate',terminal('(.venv) C:\\proyectos\\api_django_rest>'),'Si PowerShell bloquea el script, revisa la política de ejecución del usuario o activa desde CMD.'),
    D('Instalar','Instalar Django, DRF y PostgreSQL','Instala Django, Django REST Framework, el driver moderno psycopg, python-dotenv para leer .env y django-filter para filtros posteriores.','python -m pip install django djangorestframework "psycopg[binary]" python-dotenv django-filter\npython -m pip freeze > requirements.txt',terminal('Successfully installed django ...\ndjangorestframework ...\npsycopg ...'),'requirements.txt permite que otro estudiante reproduzca las dependencias con pip install -r requirements.txt.'),
    D('Proyecto','Crear el proyecto Django','startproject crea la configuración general. El punto final indica que los archivos se creen en la carpeta actual y evita una carpeta duplicada.','django-admin startproject config .\n\n# después\npython manage.py runserver',terminal('Watching for file changes with StatReloader\nStarting development server at http://127.0.0.1:8000/'),'manage.py es el punto de entrada de administración del proyecto.'),
    D('App','Crear la aplicación productos','Una app agrupa una funcionalidad. Para el ejemplo usaremos productos. startapp crea models.py, views.py, admin.py, apps.py, tests.py y migrations/.','python manage.py startapp productos',terminal('api_django_rest/\n├─ manage.py\n├─ config/\n└─ productos/'),'serializers.py y urls.py no vienen por defecto: los crearás cuando hagan falta.')
  ]);

  add('Django REST · 3. Estructura de carpetas','Aprende para qué sirve cada archivo antes de escribir el CRUD. Esta parte es especialmente importante para estudiantes que apenas comienzan.','“Saber en qué archivo va una responsabilidad evita convertir el proyecto en una sola carpeta de código.”','Explica qué archivo modificarías para cambiar la base de datos, crear una tabla y exponer una URL.',[
    D('Árbol','Estructura inicial recomendada','La raíz contiene manage.py y archivos de configuración del entorno. config es el proyecto; productos es la app. Crearemos serializers.py y urls.py manualmente para mantener responsabilidades separadas.','api_django_rest/\n├── .venv/                 # NO subir a Git\n├── .env                   # secretos locales\n├── .gitignore\n├── requirements.txt\n├── manage.py\n├── config/\n│   ├── __init__.py\n│   ├── settings.py        # configuración\n│   ├── urls.py            # rutas principales\n│   ├── asgi.py\n│   └── wsgi.py\n└── productos/\n    ├── __init__.py\n    ├── admin.py           # panel administrativo\n    ├── apps.py\n    ├── migrations/        # historial del esquema\n    │   └── __init__.py\n    ├── models.py          # tablas y reglas de datos\n    ├── serializers.py     # crear manualmente\n    ├── urls.py            # opcional, crear manualmente\n    ├── views.py           # endpoints\n    └── tests.py',terminal('config/ = configuración del proyecto\nproductos/ = funcionalidad de productos'),'No borres migrations/ para “arreglar” errores; las migraciones son parte del código del proyecto.'),
    D('settings.py','Archivo: config/settings.py','Contiene aplicaciones instaladas, conexión de base de datos, idioma, zona horaria, middleware y configuración general. Aquí registraremos rest_framework, django_filters y productos.','INSTALLED_APPS = [\n    # apps de Django...\n    "rest_framework",\n    "django_filters",\n    "productos",\n]',panel('config/settings.py','Configuración global. No pongas aquí lógica de endpoints.'),'Las credenciales no se escriben directamente en settings.py; se leen desde .env.'),
    D('models.py','Archivo: productos/models.py','Define las entidades persistentes usando el ORM de Django. Cada clase Model normalmente se convierte en una tabla y cada Field en una columna.','class Producto(models.Model):\n    nombre = models.CharField(max_length=120)\n    precio = models.DecimalField(max_digits=10, decimal_places=2)',panel('productos/models.py','Describe datos y restricciones de dominio cercanas al modelo.'),'Después de cambiar models.py normalmente debes crear y aplicar una migración.'),
    D('serializers.py','Archivo: productos/serializers.py','El serializer traduce entre objetos/modelos y JSON. También valida la entrada. Es parecido a un formulario para APIs: define qué campos entran, cuáles salen y qué reglas deben cumplir.','class ProductoSerializer(serializers.ModelSerializer):\n    class Meta:\n        model = Producto\n        fields = ["id", "nombre", "precio", "stock"]',panel('productos/serializers.py','Validación y representación JSON de la API.'),'No expongas campos internos o sensibles solo porque existen en el modelo.'),
    D('views.py','Archivo: productos/views.py','Recibe la petición HTTP y coordina la operación. Al principio veremos APIView para entender GET y POST. Luego usaremos ModelViewSet para construir el CRUD completo con menos código.','class ProductoViewSet(ModelViewSet):\n    queryset = Producto.objects.all()\n    serializer_class = ProductoSerializer',panel('productos/views.py','Entrada HTTP → reglas → serializer/ORM → respuesta.'),'Evita meter consultas duplicadas y reglas grandes en cada método; extrae servicios cuando el proyecto crezca.'),
    D('urls.py','Archivos de rutas','config/urls.py es el mapa principal del proyecto. Puede incluir rutas de cada app. Con ViewSets, un router de DRF puede generar automáticamente las URLs del CRUD.','urlpatterns = [\n    path("admin/", admin.site.urls),\n    path("api/", include("productos.urls")),\n]',flow('URL del navegador\n↓\nconfig/urls.py\n↓\nproductos/urls.py o Router\n↓\nView'),'Mantén un prefijo como /api/ y, en proyectos reales, considera versión /api/v1/.')
  ]);

  add('Django REST · 4. PostgreSQL desde cero','Instala PostgreSQL, crea un usuario y una base de datos exclusiva para la API y prueba la conexión antes de configurar Django.','“Primero confirma que PostgreSQL funciona por sí solo; después conecta el framework.”','Crea productos_db, entra con api_user y ejecuta SELECT current_database();.',[
    D('Instalación','Instalar PostgreSQL','En Windows puedes usar el instalador oficial que incluye el servidor y pgAdmin. En Ubuntu/Debian se instala desde apt. Durante la instalación recuerda la contraseña del usuario administrador postgres.','# Ubuntu / Debian\nsudo apt update\nsudo apt install postgresql postgresql-contrib\nsudo systemctl enable --now postgresql\n\n# comprobar\nsudo systemctl status postgresql',terminal('postgresql.service - PostgreSQL RDBMS\nActive: active (exited/running)'),'En Windows comprueba en Servicios que PostgreSQL esté iniciado si no puedes conectar.'),
    D('psql','Entrar a la consola','psql es el cliente de terminal de PostgreSQL. En Linux normalmente puedes entrar como el usuario del sistema postgres; en Windows usa psql o SQL Shell con el usuario postgres.','# Linux\nsudo -u postgres psql\n\n# conexión explícita\npsql -h localhost -U postgres -d postgres',terminal('postgres=#'),'Salir de psql: \\q. Ver bases: \\l. Ver roles: \\du.'),
    D('Usuario','Crear el usuario de la aplicación','No uses el superusuario postgres desde la aplicación. Crea un usuario específico con contraseña y úsalo únicamente para esta base de datos.','CREATE USER api_user WITH PASSWORD \'api123\';\n\n-- comprobar\n\\du',terminal('Role name | Attributes\napi_user  |'),'La contraseña del ejemplo es pedagógica. En un proyecto real usa una contraseña fuerte y secreta.'),
    D('Base','Crear la base de datos','Crea productos_db y deja api_user como propietario. De esta forma Django podrá crear sus tablas mediante migraciones.','CREATE DATABASE productos_db OWNER api_user;\n\n-- opcional si ya existe con otro dueño\nALTER DATABASE productos_db OWNER TO api_user;\n\n\\l',terminal('productos_db | api_user | UTF8'),'Django crea tablas; no necesitas diseñarlas manualmente en pgAdmin.'),
    D('Prueba','Probar la conexión antes de Django','Abandona la sesión de administrador e intenta entrar exactamente con las credenciales que usará la aplicación. Si esto falla, Django también fallará.','psql -h localhost -U api_user -d productos_db\n\nSELECT current_database(), current_user;\n\\dt',terminal('current_database | current_user\nproductos_db    | api_user\n\nDid not find any relations.'),'Que no existan tablas todavía es correcto: las migraciones de Django las crearán.')
  ]);

  add('Django REST · 5. Configurar .env y settings.py','Conecta Django a PostgreSQL sin dejar contraseñas escritas en el código fuente.','“Configuración no es código de negocio y los secretos no pertenecen al repositorio.”','Consigue que python manage.py migrate termine sin errores usando las variables del .env.',[
    D('.env','Archivo: .env en la raíz','Crea .env junto a manage.py. python-dotenv cargará estas variables. Este archivo contiene credenciales locales y debe estar ignorado por Git.','DB_NAME=productos_db\nDB_USER=api_user\nDB_PASSWORD=api123\nDB_HOST=localhost\nDB_PORT=5432\nDJANGO_DEBUG=True',panel('.env','Solo valores de configuración. No lo publiques en GitHub.'),'Para producción usa las variables del entorno del servidor y DJANGO_DEBUG=False.'),
    D('.gitignore','Archivo: .gitignore','Evita subir el entorno virtual, credenciales, cachés y archivos generados. .gitignore no elimina del historial archivos que ya fueron confirmados previamente.','.venv/\n.env\n__pycache__/\n*.py[cod]\n.vscode/\n.idea/',terminal('git status\n# .env y .venv ya no aparecen como archivos nuevos'),'Si .env ya fue commit, quitarlo del archivo no borra el secreto del historial; cambia la contraseña.'),
    D('Cargar entorno','Archivo: config/settings.py','Importa os y load_dotenv. BASE_DIR ya existe en el archivo generado por Django. Carga .env antes de leer las variables.','from pathlib import Path\nimport os\nfrom dotenv import load_dotenv\n\nBASE_DIR = Path(__file__).resolve().parent.parent\nload_dotenv(BASE_DIR / ".env")',panel('Orden','Primero load_dotenv(...), después os.getenv(...).'),'No copies una segunda definición de BASE_DIR si ya está creada en settings.py.'),
    D('DATABASES','Reemplazar la base SQLite por PostgreSQL','Busca DATABASES en config/settings.py y reemplázala. Django usa su backend PostgreSQL y psycopg hace la comunicación con el servidor.','DATABASES = {\n    "default": {\n        "ENGINE": "django.db.backends.postgresql",\n        "NAME": os.getenv("DB_NAME"),\n        "USER": os.getenv("DB_USER"),\n        "PASSWORD": os.getenv("DB_PASSWORD"),\n        "HOST": os.getenv("DB_HOST", "localhost"),\n        "PORT": os.getenv("DB_PORT", "5432"),\n    }\n}',flow('Django ORM → backend PostgreSQL → psycopg → PostgreSQL'),'No uses ENGINE sqlite3 si tu objetivo es PostgreSQL; revisa que no hayan quedado dos bloques DATABASES.'),
    D('Apps','Registrar DRF, filtros y productos','Agrega las aplicaciones al arreglo INSTALLED_APPS. Si productos no está registrada, sus modelos no participarán correctamente en el proyecto.','INSTALLED_APPS = [\n    "django.contrib.admin",\n    "django.contrib.auth",\n    "django.contrib.contenttypes",\n    "django.contrib.sessions",\n    "django.contrib.messages",\n    "django.contrib.staticfiles",\n    "rest_framework",\n    "django_filters",\n    "productos",\n]',panel('INSTALLED_APPS','Django conoce ahora DRF y la app productos.'),'Mantén una sola entrada por aplicación.'),
    D('Migraciones base','Crear tablas internas de Django','Antes de tu modelo, aplica las migraciones incluidas por Django para usuarios, sesiones y administración. Si la conexión está bien, las tablas aparecerán en PostgreSQL.','python manage.py migrate',terminal('Applying contenttypes.0001_initial... OK\nApplying auth.0001_initial... OK\nApplying admin.0001_initial... OK'),'Si falla aquí, resuelve primero credenciales, host, puerto o permisos de PostgreSQL.')
  ]);

  add('Django REST · 6. Modelo y migraciones','Define Producto con tipos adecuados y deja que Django convierta el modelo Python en una tabla PostgreSQL.','“models.py describe el estado deseado; las migraciones registran cómo llegar a ese estado.”','Crea Producto, aplica migraciones y verifica la tabla con \\d productos_producto.',[
    D('Modelo','Archivo: productos/models.py','Creamos un producto con nombre, SKU único, descripción opcional, precio decimal, stock entero, estado activo y fecha automática. DecimalField es preferible a float para cantidades monetarias.','from django.db import models\n\nclass Producto(models.Model):\n    nombre = models.CharField(max_length=120)\n    sku = models.CharField(max_length=30, unique=True)\n    descripcion = models.TextField(blank=True)\n    precio = models.DecimalField(max_digits=10, decimal_places=2)\n    stock = models.PositiveIntegerField(default=0)\n    activo = models.BooleanField(default=True)\n    creado_en = models.DateTimeField(auto_now_add=True)\n\n    class Meta:\n        ordering = ["nombre"]\n\n    def __str__(self):\n        return f"{self.sku} - {self.nombre}"',panel('Tabla esperada','Django generará una tabla parecida a productos_producto con id automático y las columnas declaradas.'),'unique=True en sku también crea una restricción en la base de datos.'),
    D('makemigrations','Crear el archivo de migración','makemigrations compara los modelos con el historial de migraciones y crea Python que describe el cambio. Todavía no modifica PostgreSQL.','python manage.py makemigrations productos',terminal('Migrations for \'productos\':\n  productos/migrations/0001_initial.py\n    + Create model Producto'),'Revisa la migración generada; debe formar parte de Git.'),
    D('migrate','Aplicar la migración','migrate ejecuta los cambios pendientes contra la base configurada. Ahora sí aparece la tabla de productos en PostgreSQL.','python manage.py migrate',terminal('Applying productos.0001_initial... OK'),'No edites la base manualmente para reflejar cambios cotidianos del modelo; crea migraciones.'),
    D('Verificar','Comprobar desde psql','Entra a productos_db y revisa las tablas y columnas. Así confirmas que realmente estás trabajando con PostgreSQL y no con una base accidental.','psql -h localhost -U api_user -d productos_db\n\\dt\n\\d productos_producto\nSELECT * FROM productos_producto;',terminal('Schema | Name               | Type  | Owner\npublic | productos_producto | table | api_user'),'Si sigues viendo db.sqlite3 pero tus tablas nuevas aparecen en PostgreSQL, puedes eliminar el archivo SQLite si ya no se usa.')
  ]);

  add('Django REST · 7. Admin de Django','Usa el panel administrativo para comprobar el modelo antes de construir toda la API.','“El admin no reemplaza la API, pero es una excelente herramienta de verificación y gestión interna.”','Crea un superusuario, registra Producto y agrega dos productos desde /admin/.',[
    D('admin.py','Archivo: productos/admin.py','Registra el modelo para que aparezca en el panel. ModelAdmin permite configurar columnas, búsqueda y filtros sin construir una pantalla desde cero.','from django.contrib import admin\nfrom .models import Producto\n\n@admin.register(Producto)\nclass ProductoAdmin(admin.ModelAdmin):\n    list_display = ("id", "sku", "nombre", "precio", "stock", "activo")\n    search_fields = ("sku", "nombre")\n    list_filter = ("activo",)',panel('/admin/','Podrás listar, crear, editar y eliminar productos desde el panel administrativo.'),'El admin suele ser para personal interno, no para usuarios finales de la aplicación.'),
    D('superuser','Crear el usuario administrador','Django incluye autenticación. Crea un superusuario y proporciona nombre, correo opcional y contraseña cuando la terminal lo solicite.','python manage.py createsuperuser\npython manage.py runserver',terminal('Username: admin\nEmail address: ...\nPassword: ...\nSuperuser created successfully.'),'Abre http://127.0.0.1:8000/admin/ y verifica que Productos aparezca.'),
    D('Comprobar','Validar persistencia','Crea un producto en el admin y luego consulta PostgreSQL. Esto demuestra que el formulario administrativo y el ORM están guardando datos en productos_db.','SELECT id, sku, nombre, precio, stock\nFROM productos_producto;',terminal('1 | PAN-01 | Pan artesanal | 2500.00 | 20'),'Si el admin guarda correctamente, modelo, migración y base de datos ya están alineados.')
  ]);

  add('Django REST · 8. Serializer','Transforma modelos a JSON y valida el JSON de entrada antes de guardarlo.','“El serializer es la frontera entre datos externos y objetos internos.”','Valida que precio sea mayor que cero y prueba un JSON inválido.',[
    D('Crear archivo','Archivo nuevo: productos/serializers.py','startapp no crea serializers.py. Créalo manualmente dentro de productos. ModelSerializer puede generar campos y operaciones básicas a partir del modelo.','from rest_framework import serializers\nfrom .models import Producto\n\nclass ProductoSerializer(serializers.ModelSerializer):\n    class Meta:\n        model = Producto\n        fields = [\n            "id", "sku", "nombre", "descripcion",\n            "precio", "stock", "activo", "creado_en"\n        ]\n        read_only_fields = ["id", "creado_en"]',panel('Entrada','JSON → ProductoSerializer → datos validados → modelo\n\nSalida: modelo → ProductoSerializer → JSON'),'read_only_fields evita que el cliente intente asignar id o creado_en.'),
    D('Validación','Validación de un campo','Un método validate_precio se ejecuta automáticamente para precio. Si la regla falla, DRF devuelve un 400 con un mensaje útil y no guarda el producto.','def validate_precio(self, value):\n    if value <= 0:\n        raise serializers.ValidationError(\n            "El precio debe ser mayor que cero."\n        )\n    return value',panel('JSON inválido','{ "precio": -500 }\n→ 400 Bad Request\n→ {"precio":["El precio debe ser mayor que cero."]}'),'Las reglas importantes también pueden apoyarse en restricciones del modelo/base de datos.'),
    D('Validación objeto','Validar varios campos juntos','validate(self, attrs) permite reglas que dependen de más de un campo. Por ejemplo, un producto inactivo podría requerir stock cero según una regla concreta del negocio.','def validate(self, attrs):\n    activo = attrs.get("activo", True)\n    stock = attrs.get("stock", 0)\n    if not activo and stock > 0:\n        raise serializers.ValidationError(\n            "Un producto inactivo debe tener stock 0."\n        )\n    return attrs',flow('request.data → serializer.is_valid() → validated_data'),'No inventes reglas solo por usar validate; deben representar necesidades reales del sistema.')
  ]);

  add('Django REST · 9. Primer GET y POST con APIView','Construye endpoints manualmente para entender qué hace DRF antes de usar automatizaciones.','“Primero escribe el flujo explícito; después aprecia lo que ModelViewSet simplifica.”','Crea GET y POST para /api/productos/manual/ y pruébalos desde el navegador.',[
    D('APIView','Archivo: productos/views.py','APIView te entrega request.data, Response y manejo de contenido JSON. En GET consultamos el ORM; en POST validamos y guardamos mediante el serializer.','from rest_framework.views import APIView\nfrom rest_framework.response import Response\nfrom rest_framework import status\nfrom .models import Producto\nfrom .serializers import ProductoSerializer\n\nclass ProductoListCreateAPIView(APIView):\n    def get(self, request):\n        productos = Producto.objects.all()\n        serializer = ProductoSerializer(productos, many=True)\n        return Response(serializer.data)\n\n    def post(self, request):\n        serializer = ProductoSerializer(data=request.data)\n        serializer.is_valid(raise_exception=True)\n        serializer.save()\n        return Response(serializer.data, status=status.HTTP_201_CREATED)',panel('Comportamiento','GET → lista JSON\nPOST + JSON válido → 201 + producto creado\nPOST inválido → 400 + errores'),'many=True es obligatorio cuando serializas una colección de objetos.'),
    D('urls app','Archivo nuevo: productos/urls.py','Crea urls.py manualmente. as_view() convierte la clase APIView en una vista que Django puede ejecutar.','from django.urls import path\nfrom .views import ProductoListCreateAPIView\n\nurlpatterns = [\n    path(\n        "productos/manual/",\n        ProductoListCreateAPIView.as_view(),\n        name="producto-list-create-manual"\n    ),\n]',flow('/api/productos/manual/ → ProductoListCreateAPIView'),'Mantendremos este endpoint solo con fines pedagógicos; luego el router generará el CRUD principal.'),
    D('urls proyecto','Archivo: config/urls.py','Incluye las rutas de productos bajo /api/. La ruta final combina el prefijo del proyecto y la ruta de la app.','from django.contrib import admin\nfrom django.urls import include, path\n\nurlpatterns = [\n    path("admin/", admin.site.urls),\n    path("api/", include("productos.urls")),\n]',panel('URL final','http://127.0.0.1:8000/api/productos/manual/'),'Un 404 suele significar que la URL no está incluida o que la ruta escrita no coincide.'),
    D('Probar','Usar la interfaz navegable','Con runserver activo abre la URL. DRF muestra una interfaz HTML que permite ver GET y enviar POST. La misma URL devuelve JSON si el cliente lo solicita.','python manage.py runserver\n\n# navegador\nhttp://127.0.0.1:8000/api/productos/manual/',panel('POST de ejemplo','{<br>"sku":"CAF-01",<br>"nombre":"Café",<br>"precio":"12000.00",<br>"stock":8<br>}'),'La interfaz navegable sirve para aprender y depurar; Postman/curl también son útiles.')
  ]);

  add('Django REST · 10. CRUD completo con ModelViewSet','Después de entender APIView, reduce código repetido usando ModelViewSet y deja que DRF implemente operaciones CRUD estándar.','“La abstracción es útil cuando ya entiendes lo que está abstraído.”','Reemplaza el endpoint pedagógico por un router y prueba las seis operaciones generadas.',[
    D('ViewSet','Archivo: productos/views.py','ModelViewSet combina list, retrieve, create, update, partial_update y destroy. Solo necesita saber qué queryset usar y qué serializer representa el recurso.','from rest_framework.viewsets import ModelViewSet\nfrom .models import Producto\nfrom .serializers import ProductoSerializer\n\nclass ProductoViewSet(ModelViewSet):\n    queryset = Producto.objects.all()\n    serializer_class = ProductoSerializer',panel('ModelViewSet','Una clase → CRUD completo para Producto.'),'Cuando tengas reglas específicas, sobrescribe métodos concretos o extrae servicios; no copies otro CRUD manual entero.'),
    D('Router','Archivo: productos/urls.py','DefaultRouter registra el ViewSet y genera las rutas automáticamente. No necesitas escribir una path por cada método HTTP.','from rest_framework.routers import DefaultRouter\nfrom .views import ProductoViewSet\n\nrouter = DefaultRouter()\nrouter.register("productos", ProductoViewSet, basename="producto")\n\nurlpatterns = router.urls',panel('Rutas','GET /api/productos/\nPOST /api/productos/\nGET /api/productos/{id}/\nPUT /api/productos/{id}/\nPATCH /api/productos/{id}/\nDELETE /api/productos/{id}/'),'Mantén el nombre del recurso en plural para colecciones.'),
    D('Endpoints','Qué operación genera cada combinación','El router distingue colección y detalle. POST crea y devuelve 201; GET detalle devuelve 200 o 404; DELETE normalmente devuelve 204 sin cuerpo.','GET    /api/productos/      → list\nPOST   /api/productos/      → create\nGET    /api/productos/1/    → retrieve\nPUT    /api/productos/1/    → update\nPATCH  /api/productos/1/    → partial_update\nDELETE /api/productos/1/    → destroy',terminal('HTTP/1.1 201 Created\nContent-Type: application/json'),'Aprende la diferencia entre PUT completo y PATCH parcial.'),
    D('Ejemplo POST','Crear un producto','El JSON pasa por ProductoSerializer. Si sku ya existe, la restricción unique y el serializer generan un error en vez de crear un duplicado.','POST /api/productos/\nContent-Type: application/json\n\n{\n  "sku": "TEC-01",\n  "nombre": "Teclado",\n  "descripcion": "Teclado mecánico",\n  "precio": "180000.00",\n  "stock": 12,\n  "activo": true\n}',panel('Respuesta 201','{<br>"id":3,<br>"sku":"TEC-01",<br>"nombre":"Teclado",<br>"precio":"180000.00",<br>"stock":12,<br>"activo":true<br>}'),'Envía también un precio negativo para confirmar que la validación responde 400.')
  ]);

  add('Django REST · 11. Filtros, búsqueda y orden','Haz que el listado sea útil cuando ya existen muchos registros.','“Un buen endpoint de lista no obliga al frontend a descargar toda la tabla para encontrar un registro.”','Implementa búsqueda por nombre/SKU y filtrado por activo.',[
    D('Config','Configurar los backends de filtros','En settings.py define filtros globales de DRF. django-filter permite filtros exactos; SearchFilter hace búsquedas de texto y OrderingFilter controla el orden solicitado.','REST_FRAMEWORK = {\n    "DEFAULT_FILTER_BACKENDS": [\n        "django_filters.rest_framework.DjangoFilterBackend",\n        "rest_framework.filters.SearchFilter",\n        "rest_framework.filters.OrderingFilter",\n    ]\n}',panel('Disponibles','?activo=true\n?search=teclado\n?ordering=-precio'),'La configuración global evita repetir los mismos backends en cada ViewSet.'),
    D('ViewSet','Declarar campos permitidos','En ProductoViewSet indica qué campos se filtran, buscan y ordenan. Limitar ordering_fields evita ordenar por campos que no quieres exponer.','class ProductoViewSet(ModelViewSet):\n    queryset = Producto.objects.all()\n    serializer_class = ProductoSerializer\n    filterset_fields = ["activo", "sku"]\n    search_fields = ["nombre", "sku", "descripcion"]\n    ordering_fields = ["nombre", "precio", "stock", "creado_en"]\n    ordering = ["nombre"]',panel('Ejemplos','/api/productos/?activo=true<br>/api/productos/?search=cafe<br>/api/productos/?ordering=-precio'),'Los query parameters modifican la consulta, no la ruta del recurso.'),
    D('ORM','Qué termina ejecutando el ORM','Django traduce los filtros del QuerySet a SQL parametrizado. Normalmente no necesitas concatenar SQL manualmente para búsquedas comunes.','Producto.objects.filter(\n    activo=True,\n    nombre__icontains="cafe"\n).order_by("-precio")',flow('Query params → FilterBackend → QuerySet → SQL → PostgreSQL'),'Para consultas complejas revisa el SQL generado y crea índices según el patrón de uso, no por intuición.')
  ]);

  add('Django REST · 12. Probar la API','Prueba cada endpoint desde varias herramientas y aprende a interpretar método, URL, código de estado y cuerpo.','“Una API no está terminada porque el servidor inició; debe responder correctamente a casos válidos e inválidos.”','Crea, lista, modifica y elimina un producto usando curl o Postman.',[
    D('Navegador','Browsable API de DRF','GET se puede visitar directamente. La interfaz de DRF también genera formularios para POST, PUT y PATCH según el serializer y permite observar errores de validación.','http://127.0.0.1:8000/api/productos/',panel('Ventaja pedagógica','Puedes ver método, URL, campos esperados, respuesta JSON y estado en una misma pantalla.'),'Más adelante podrás desactivar o limitar renderizadores HTML si tu API pública solo necesita JSON.'),
    D('curl GET','Listar desde terminal','curl es útil porque muestra una petición reproducible sin depender de una interfaz gráfica.','curl http://127.0.0.1:8000/api/productos/',terminal('[{"id":1,"sku":"PAN-01","nombre":"Pan"...}]'),'Agrega -i para ver también headers y código HTTP.'),
    D('curl POST','Crear desde terminal','Indica el método, Content-Type y cuerpo JSON. En Windows PowerShell curl puede ser un alias; curl.exe fuerza el ejecutable clásico.','curl -X POST http://127.0.0.1:8000/api/productos/ \\\n  -H "Content-Type: application/json" \\\n  -d \'{"sku":"MOU-01","nombre":"Mouse","precio":"65000.00","stock":10}\'',terminal('HTTP 201\n{"id":4,"sku":"MOU-01","nombre":"Mouse",...}'),'En PowerShell adapta las comillas o usa Postman/Thunder Client si el escape te distrae del concepto HTTP.'),
    D('Casos','Casos mínimos a probar','No pruebes solamente el camino feliz. Incluye SKU duplicado, precio inválido, id inexistente, JSON mal formado y actualización parcial. Cada caso debe producir un código y mensaje coherentes.','201 → creación válida\n200 → consulta/actualización válida\n204 → eliminación válida\n400 → validación fallida\n404 → id inexistente',panel('Checklist','✓ crear<br>✓ listar<br>✓ consultar uno<br>✓ patch<br>✓ delete<br>✓ datos inválidos<br>✓ recurso inexistente'),'Guarda ejemplos de request/response para que los estudiantes puedan comparar resultados.')
  ]);

  add('Django REST · 13. Conectar un frontend','Permite que Angular, React u otra interfaz consuma la API y comprende por qué aparece CORS.','“El navegador aplica CORS; tu API debe autorizar explícitamente los orígenes permitidos.”','Permite temporalmente http://localhost:4200 y consulta productos desde fetch.',[
    D('CORS','Instalar django-cors-headers','Si el frontend corre en otro origen, por ejemplo Angular en localhost:4200, el navegador exige cabeceras CORS. Instala la biblioteca y configúrala de forma explícita.','python -m pip install django-cors-headers\npython -m pip freeze > requirements.txt',terminal('Successfully installed django-cors-headers'),'CORS no es autenticación; solo controla qué orígenes del navegador pueden leer respuestas.'),
    D('settings.py','Configurar orígenes permitidos','Registra corsheaders y su middleware. Para desarrollo autoriza solo los orígenes usados por tu frontend en vez de habilitar todos indiscriminadamente.','INSTALLED_APPS += ["corsheaders"]\n\nMIDDLEWARE = [\n    "corsheaders.middleware.CorsMiddleware",\n    *MIDDLEWARE,\n]\n\nCORS_ALLOWED_ORIGINS = [\n    "http://localhost:4200",\n    "http://localhost:5173",\n]',panel('Desarrollo','Angular 4200 y Vite 5173 pueden consumir la API.'),'Asegúrate de no duplicar MIDDLEWARE; inserta CorsMiddleware cerca del inicio del arreglo existente.'),
    D('Frontend','Ejemplo con fetch','El frontend llama a la API HTTP; nunca conoce usuario ni contraseña de PostgreSQL. La API responde JSON y el navegador lo transforma en objetos JavaScript.','const respuesta = await fetch(\n  "http://127.0.0.1:8000/api/productos/"\n);\n\nif (!respuesta.ok) {\n  throw new Error("No se pudo cargar productos");\n}\n\nconst productos = await respuesta.json();\nconsole.log(productos);',flow('Angular / React → HTTP → Django REST → PostgreSQL'),'Centraliza la URL base en configuración del frontend y no la repitas en cada componente.')
  ]);

  add('Django REST · 14. Errores comunes','Aprende a leer el mensaje antes de cambiar archivos. La mayoría de problemas iniciales pertenecen a instalación, rutas, migraciones o PostgreSQL.','“El traceback no es un castigo: es un mapa que indica dónde empezó el problema.”','Provoca un 404 y un 400 intencionalmente y explica por qué son respuestas diferentes.',[
    D('Módulo','ModuleNotFoundError','Si aparece No module named rest_framework, psycopg o dotenv, normalmente el entorno virtual no está activo o la dependencia no fue instalada en ese entorno.','python -m pip show djangorestframework\npython -m pip show psycopg\npython -m pip show python-dotenv\n\npython -m pip install -r requirements.txt',terminal('Name: djangorestframework\nVersion: ...'),'Confirma qué python ejecutas con where python en Windows o which python en Linux.'),
    D('Conexión','connection refused','PostgreSQL no está escuchando en host/puerto indicado, el servicio está detenido o DB_HOST/DB_PORT son incorrectos.','psql -h localhost -p 5432 -U api_user -d productos_db\n\n# Linux\nsudo systemctl status postgresql',terminal('psql: error: connection to server ... failed'),'Prueba psql con las mismas credenciales antes de culpar a Django.'),
    D('Contraseña','password authentication failed','El servidor sí responde, pero rechazó las credenciales. Revisa DB_USER/DB_PASSWORD y que .env se haya cargado.','python manage.py shell\n>>> import os\n>>> os.getenv("DB_USER")\n\'api_user\'',terminal('FATAL: password authentication failed for user "api_user"'),'No imprimas contraseñas reales en capturas, logs o repositorios.'),
    D('Tabla','relation does not exist','La conexión funciona pero falta una tabla, normalmente porque la migración no fue creada/aplicada o estás conectado a otra base.','python manage.py showmigrations\npython manage.py makemigrations\npython manage.py migrate\n\n# PostgreSQL\n\\dt',terminal('[X] 0001_initial'),'No crees la tabla manualmente para esconder el error; corrige el flujo de migraciones.'),
    D('404','Not Found','Django no encontró una ruta o DRF no encontró un objeto. Distingue si el 404 ocurre antes de entrar a la ViewSet o al consultar /productos/999/.','# Revisa config/urls.py, productos/urls.py y router.urls\n# Después prueba la URL exacta',panel('Dos 404','Ruta inexistente: URL mal configurada.<br>Objeto inexistente: endpoint existe pero el id no.'),'Revisa siempre la URL exacta, incluyendo la barra final si APPEND_SLASH está activo.'),
    D('400','Bad Request','La petición llegó al endpoint, pero el serializer rechazó los datos. El cuerpo de respuesta indica qué campo falló.','{\n  "sku": ["producto con este sku ya existe."],\n  "precio": ["El precio debe ser mayor que cero."]\n}',panel('400 ≠ error del servidor','Es una respuesta esperada cuando el cliente envía datos inválidos.'),'En el frontend muestra estos mensajes de forma útil en lugar de reemplazarlos por “algo salió mal”.')
  ]);

  add('Django REST · 15. Proyecto final y siguiente nivel','Cierra el recorrido con una estructura limpia, comandos reproducibles y retos que obligan a modificar el proyecto sin copiar una solución completa.','“Aprender backend significa poder explicar qué archivo cambia, por qué cambia y cómo comprobar que funcionó.”','Amplía la API con categorías y registra el cambio mediante una nueva migración.',[
    D('Estructura final','Así debería verse el proyecto','La estructura final separa configuración, app, serializers y migraciones. .env y .venv permanecen fuera de Git; requirements.txt y migrations sí se versionan.','api_django_rest/\n├── .env                    # NO Git\n├── .gitignore\n├── requirements.txt\n├── manage.py\n├── config/\n│   ├── settings.py\n│   └── urls.py\n└── productos/\n    ├── admin.py\n    ├── migrations/\n    │   ├── __init__.py\n    │   └── 0001_initial.py\n    ├── models.py\n    ├── serializers.py\n    ├── urls.py\n    ├── views.py\n    └── tests.py',terminal('python -m pip install -r requirements.txt\npython manage.py migrate\npython manage.py runserver'),'Un compañero debería poder clonar, configurar .env, instalar dependencias y ejecutar sin adivinar pasos.'),
    D('Flujo final','Resumen completo','El cliente envía JSON. URL/router selecciona el ViewSet. El serializer valida. El ORM crea SQL seguro. PostgreSQL aplica restricciones y guarda. DRF serializa el objeto resultante y responde con un código HTTP coherente.','Cliente\n  ↓ HTTP JSON\nRouter / URL\n  ↓\nViewSet\n  ↓\nSerializer\n  ↓\nDjango ORM\n  ↓\nPostgreSQL\n  ↑\nResponse JSON + status',flow('Request → URL → ViewSet → Serializer → ORM → PostgreSQL → Response'),'Si puedes narrar este flujo usando un ejemplo real, ya entendiste la base del framework.'),
    D('Reto 1','Agregar categorías','Crea el modelo Categoria con nombre único. Relaciona Producto mediante ForeignKey, crea y aplica la migración, agrega serializer y endpoint. No borres los productos existentes para lograrlo.','class Categoria(models.Model):\n    nombre = models.CharField(max_length=80, unique=True)\n\nclass Producto(models.Model):\n    categoria = models.ForeignKey(\n        Categoria,\n        on_delete=models.PROTECT,\n        related_name="productos"\n    )\n    # ...',panel('Objetivo','GET /api/categorias/<br>POST /api/categorias/<br>Producto expone su categoría.'),'Piensa qué valor usar para productos existentes cuando añades una relación obligatoria.'),
    D('Reto 2','Agregar pruebas','Usa APITestCase para comprobar que crear un producto válido devuelve 201, precio negativo devuelve 400 y consultar un id inexistente devuelve 404.','from rest_framework.test import APITestCase\nfrom rest_framework import status\n\nclass ProductoApiTests(APITestCase):\n    def test_precio_negativo(self):\n        response = self.client.post("/api/productos/", {\n            "sku": "ERR-01",\n            "nombre": "Inválido",\n            "precio": "-1.00",\n            "stock": 1\n        }, format="json")\n        self.assertEqual(response.status_code, status.HTTP_400_BAD_REQUEST)',terminal('python manage.py test\n\nRan 3 tests in ...\nOK'),'Una prueba debe verificar comportamiento observable, no solo ejecutar líneas.'),
    D('Siguiente','Después del CRUD','Cuando CRUD, PostgreSQL y pruebas estén claros, continúa con autenticación, JWT, permisos, paginación, documentación OpenAPI, Docker, transacciones, logging y despliegue. Hazlo en ese orden según las necesidades del proyecto.','Siguiente nivel:\n1. paginación\n2. autenticación + JWT\n3. permisos por rol\n4. tests más completos\n5. Docker\n6. CI/CD\n7. despliegue',panel('Regla','No agregues complejidad antes de poder explicar y probar el flujo básico.'),'Compara esta implementación con las secciones Backend FastAPI de la misma biblioteca para reforzar conceptos transferibles.')
  ]);

  buildNav();
  render();
})();
