(()=>{
  if(window.__backendNodeOrmsAdded)return;
  window.__backendNodeOrmsAdded=true;
  if(!Array.isArray(window.sections)||typeof window.T!=='function')return;

  const preview=(title,html)=>`<section style="font-family:system-ui;padding:16px;border:1px solid #334155;border-radius:12px;background:#07111f;color:#e5edf8"><strong style="display:block;margin-bottom:8px;color:#34d399">${title}</strong>${html}</section>`;
  const file=(path,detail,method='MANUAL',command='')=>({path,method,detail,command});
  const O=(topic,name,description,code,previewHtml,tip,meta={})=>T(topic,name,description,code,previewHtml,[],{
    kind:'ORM Node.js',tip,
    guideTitle:'Dónde se hace cada modificación',
    codeLabel:'Código / esquema',
    filesToCreateTitle:'Archivos que crea el estudiante',
    filesToCreateStatus:'Usa una base de práctica. Versiona migraciones y esquemas; no subas credenciales reales al repositorio.',
    ...meta
  });

  sections.push({
    title:'Backend · ORM Node.js · Prisma vs Sequelize vs Drizzle',
    navLabel:'ORM Node.js · Prisma vs Sequelize vs Drizzle',
    group:'Backend',primaryArea:'Backend',course:'Backend',areaOrder:34,
    description:'Aprende qué resuelve un ORM en Node.js y compara Prisma, Sequelize y Drizzle con ejemplos reales: modelos, migraciones, consultas, relaciones, transacciones, SQL, TypeScript y organización de archivos.',
    quote:'“Un ORM no elimina SQL: cambia cómo modelas, consultas y mantienes el acceso a datos.”',
    challenge:'Implementa usuarios y tareas con PostgreSQL usando uno de los tres enfoques. Debe incluir migración, relación 1:N, consulta filtrada y transacción.',
    items:[
      O('concepto','1. Qué es un ORM y qué problema resuelve','Un ORM conecta objetos o estructuras de tu aplicación con tablas relacionales. Puede ayudarte con modelos, tipos, consultas, relaciones y migraciones. Aun así debes entender claves, índices, constraints, transacciones y SQL porque el rendimiento y la integridad dependen de la base, no del ORM.', 'APP Node.js\n   ↓\nORM / query layer\n   ↓\nSQL\n   ↓\nPostgreSQL\n\nDebes seguir entendiendo:\n- PK / FK\n- índices\n- JOIN\n- transacciones\n- constraints\n- EXPLAIN',preview('Modelo mental','<p style="margin:0">ORM = capa de acceso a datos; la base sigue ejecutando SQL.</p>'),'Aprende primero el modelo relacional; después compara ergonomía y herramientas.',{
        guide:[['Crear','orm-comparison/README.md','Define entidades, relaciones y consultas antes de elegir herramienta.']],filesToCreate:[file('orm-comparison/README.md','Mapa de requisitos.')]
      }),
      O('comparison','2. Diferencia general: Prisma, Sequelize y Drizzle','Prisma gira alrededor de un schema declarativo y un cliente generado con fuerte experiencia de tipos. Sequelize sigue un enfoque ORM tradicional con modelos, asociaciones y un ecosistema maduro. Drizzle mantiene una API muy cercana a SQL y usa esquemas TypeScript, buscando poco peso y tipado directo. Ninguno es “mejor” en todos los proyectos.', 'PRISMA\nschema.prisma → migrate → generated client\n\nSEQUELIZE\nmodels/classes → associations → queries\n\nDRIZZLE\nTypeScript schema → SQL-like queries → drizzle-kit',preview('Comparación','<p style="margin:0">Prisma → DX y cliente generado · Sequelize → ORM clásico · Drizzle → tipado + cercanía a SQL.</p>'),'Elige por modelo mental, runtime, equipo, migraciones y necesidad de SQL explícito, no solo por tamaño del paquete.',{
        guide:[['Modificar','orm-comparison/README.md','Crea una tabla comparativa con requisitos reales.']],filesToCreate:[file('orm-comparison/README.md','Decisión técnica.')]
      }),
      O('Prisma','3. Prisma · instalación y esquema','Prisma normalmente separa la definición del modelo en prisma/schema.prisma y genera un cliente para consultar. Configura DATABASE_URL fuera del código y crea modelos con constraints explícitos. Las versiones y comandos pueden evolucionar; usa la CLI instalada en tu proyecto y consulta la documentación de la versión usada.','npm install @prisma/client\nnpm install -D prisma\n\n# .env\nDATABASE_URL="postgresql://..."\n\n// prisma/schema.prisma\ndatasource db {\n  provider = "postgresql"\n  url      = env("DATABASE_URL")\n}\n\ngenerator client {\n  provider = "prisma-client-js"\n}\n\nmodel User {\n  id    Int    @id @default(autoincrement())\n  email String @unique\n  tasks Task[]\n}\n\nmodel Task {\n  id     Int     @id @default(autoincrement())\n  title  String\n  done   Boolean @default(false)\n  userId Int\n  user   User    @relation(fields: [userId], references: [id])\n}',preview('Prisma','<p style="margin:0">schema.prisma describe el modelo y Prisma Client expone consultas tipadas.</p>'),'No guardes DATABASE_URL real en Git; deja .env fuera y crea .env.example.',{
        guide:[['Ejecutar','Terminal · raíz del backend','Instala prisma y @prisma/client.'],['Crear','prisma/schema.prisma','Define datasource, generator y modelos.'],['Crear','.env','Guarda DATABASE_URL local.']],filesToCreate:[file('prisma/schema.prisma','Esquema Prisma.'),file('.env.example','Plantilla de DATABASE_URL.')]
      }),
      O('Prisma migrate','4. Prisma · migraciones y cliente','Una migración debe representar un cambio reproducible del esquema. En desarrollo crea la migración, revísala y versiona sus archivos. Después genera o actualiza el cliente según el flujo de la versión usada. En producción aplica migraciones con el comando recomendado para despliegue, no recreando la base.','npx prisma migrate dev --name init\n\n# se crea algo como\nprisma/migrations/2026..._init/migration.sql\n\n# inspecciona el SQL antes de compartirlo\nnpx prisma studio',preview('Migraciones','<p style="margin:0">Cambio en schema → migration.sql → base → cliente.</p>'),'Versiona las migraciones junto con el código para que otro entorno reproduzca el esquema.',{
        guide:[['Generar','prisma/migrations/<timestamp>_init/migration.sql','Revisa el SQL generado.'],['Ejecutar','Terminal','Aplica la migración en desarrollo.']],filesToCreate:[file('prisma/migrations/<timestamp>_init/migration.sql','Migración generada.','PRISMA','npx prisma migrate dev --name init')]
      }),
      O('Prisma queries','5. Prisma Client · CRUD, include y transacción','Prisma Client ofrece métodos como findMany, create, update y delete. include permite cargar relaciones y $transaction agrupa operaciones que deben confirmarse juntas. Evita cargar columnas o relaciones innecesarias; el ORM no impide consultas costosas.','import { PrismaClient } from "@prisma/client";\nconst prisma = new PrismaClient();\n\nconst users = await prisma.user.findMany({\n  where: { tasks: { some: { done: false } } },\n  include: { tasks: true }\n});\n\nawait prisma.$transaction(async tx => {\n  const user = await tx.user.create({\n    data: { email: "ana@example.test" }\n  });\n  await tx.task.create({\n    data: { title: "Repasar SQL", userId: user.id }\n  });\n});',preview('Prisma query','<p style="margin:0">Cliente tipado → consulta → SQL generado → PostgreSQL.</p>'),'Usa select/include con intención y revisa consultas lentas en la base.',{
        guide:[['Crear','src/db/prisma.js','Exporta una única instancia reutilizable.'],['Crear','src/repositories/users.js','Coloca consultas del dominio.']],filesToCreate:[file('src/db/prisma.js','Cliente Prisma.'),file('src/repositories/users.js','Consultas de usuarios.')]
      }),
      O('Sequelize','6. Sequelize · conexión y modelo','Sequelize representa tablas mediante modelos y permite trabajar con asociaciones. La API exacta depende de la versión usada; en un proyecto real fija versiones y sigue su documentación. Separa la conexión de la definición de modelos para no mezclar infraestructura y dominio.','npm install sequelize pg pg-hstore\n\n// src/db/sequelize.js\nimport { Sequelize, DataTypes } from "sequelize";\n\nexport const sequelize = new Sequelize(process.env.DATABASE_URL, {\n  logging: false\n});\n\nexport const User = sequelize.define("User", {\n  email: { type: DataTypes.STRING, allowNull: false, unique: true }\n});',preview('Sequelize','<p style="margin:0">Sequelize instance → models → SQL → base relacional.</p>'),'Evita usar sync({ force: true }) como sustituto de migraciones en producción.',{
        guide:[['Crear','src/db/sequelize.js','Conexión y configuración.'],['Crear','src/models/User.js','Modelo User si prefieres separar modelos.']],filesToCreate:[file('src/db/sequelize.js','Conexión.'),file('src/models/User.js','Modelo.')]
      }),
      O('Sequelize associations','7. Sequelize · asociaciones, consultas y transacciones','Las asociaciones expresan relaciones entre modelos: hasMany, belongsTo, belongsToMany, etc. include permite eager loading. Para operaciones atómicas usa transaction y pasa la transacción a cada query involucrada.','User.hasMany(Task, { foreignKey: "userId" });\nTask.belongsTo(User, { foreignKey: "userId" });\n\nconst users = await User.findAll({\n  include: [{ model: Task, where: { done: false } }]\n});\n\nawait sequelize.transaction(async transaction => {\n  const user = await User.create(\n    { email: "ana@example.test" },\n    { transaction }\n  );\n  await Task.create(\n    { title: "Repasar SQL", userId: user.id },\n    { transaction }\n  );\n});',preview('Asociación','<p style="margin:0">User 1 ─── N Task · include carga la relación.</p>'),'Revisa el SQL generado cuando combines includes, filtros y paginación.',{
        guide:[['Modificar','src/models/index.js','Registra asociaciones una vez.'],['Crear','src/repositories/users-sequelize.js','Consultas y transacciones.']],filesToCreate:[file('src/models/index.js','Asociaciones.'),file('src/repositories/users-sequelize.js','Consultas.')]
      }),
      O('Sequelize migrations','8. Sequelize · migraciones con CLI','En un equipo no conviene depender de que los modelos “sincronicen” automáticamente producción. Usa migraciones para crear y alterar tablas de forma controlada. La herramienta CLI y su configuración pueden variar por versión, así que mantén el proyecto fijado y revisa los archivos generados.','npx sequelize-cli migration:generate --name create-users\n\n// migrations/...-create-users.js\nexport async function up(queryInterface, Sequelize) {\n  await queryInterface.createTable("Users", {\n    id: { type: Sequelize.INTEGER, primaryKey: true, autoIncrement: true },\n    email: { type: Sequelize.STRING, allowNull: false, unique: true }\n  });\n}\n\nexport async function down(queryInterface) {\n  await queryInterface.dropTable("Users");\n}',preview('Migración','<p style="margin:0">up aplica el cambio · down define cómo revertirlo cuando sea seguro.</p>'),'Nunca generes una migración y la ejecutes en producción sin revisar el SQL o el efecto equivalente.',{
        guide:[['Generar','migrations/<timestamp>-create-users.js','Describe el cambio reproducible.']],filesToCreate:[file('migrations/<timestamp>-create-users.js','Migración.','SEQUELIZE CLI','npx sequelize-cli migration:generate --name create-users')]
      }),
      O('Drizzle','9. Drizzle · esquema TypeScript','Drizzle define tablas en TypeScript y mantiene una API cercana a SQL. Con PostgreSQL puedes combinar drizzle-orm con un driver compatible. Drizzle Kit ayuda a generar y aplicar migraciones según el flujo configurado.','npm install drizzle-orm pg\nnpm install -D drizzle-kit\n\n// src/db/schema.js\nimport { pgTable, serial, text, boolean, integer } from "drizzle-orm/pg-core";\n\nexport const users = pgTable("users", {\n  id: serial("id").primaryKey(),\n  email: text("email").notNull().unique()\n});\n\nexport const tasks = pgTable("tasks", {\n  id: serial("id").primaryKey(),\n  title: text("title").notNull(),\n  done: boolean("done").notNull().default(false),\n  userId: integer("user_id").notNull().references(() => users.id)\n});',preview('Drizzle schema','<p style="margin:0">TypeScript schema ↔ SQL tables con nombres explícitos.</p>'),'Si te gusta razonar en SQL, Drizzle suele hacer visibles columnas, filtros y joins sin esconder demasiado.',{
        guide:[['Crear','src/db/schema.js','Define tablas y referencias.'],['Crear','drizzle.config.js','Configura migraciones y dialecto.']],filesToCreate:[file('src/db/schema.js','Esquema.'),file('drizzle.config.js','Configuración de Drizzle Kit.')]
      }),
      O('Drizzle query','10. Drizzle · select, join y transacciones','La API de Drizzle permite construir consultas con funciones y expresiones cercanas a SQL. Puedes usar select explícito, joins y transaction. Conserva consultas complejas en repositorios o módulos de datos para que tus controladores HTTP no se llenen de detalles de persistencia.','import { eq } from "drizzle-orm";\nimport { db } from "./client.js";\nimport { users, tasks } from "./schema.js";\n\nconst rows = await db\n  .select({ email: users.email, task: tasks.title })\n  .from(users)\n  .leftJoin(tasks, eq(tasks.userId, users.id));\n\nawait db.transaction(async tx => {\n  const [user] = await tx.insert(users)\n    .values({ email: "ana@example.test" })\n    .returning();\n  await tx.insert(tasks).values({\n    title: "Repasar SQL", userId: user.id\n  });\n});',preview('Drizzle query','<p style="margin:0">select + leftJoin hace visible la forma de la consulta y el resultado esperado.</p>'),'Selecciona solo las columnas que necesita la API y añade índices según filtros reales.',{
        guide:[['Crear','src/db/client.js','Conecta driver y Drizzle.'],['Crear','src/repositories/users-drizzle.js','Consultas y transacciones.']],filesToCreate:[file('src/db/client.js','Cliente Drizzle.'),file('src/repositories/users-drizzle.js','Consultas.')]
      }),
      O('migrations','11. Drizzle Kit · migraciones','Con Drizzle Kit puedes generar migraciones desde el esquema y mantener SQL versionado. Revisa el resultado antes de aplicarlo, especialmente para cambios destructivos, renombres o columnas no nulas en tablas con datos.','// drizzle.config.js\nexport default {\n  schema: "./src/db/schema.js",\n  out: "./drizzle",\n  dialect: "postgresql",\n  dbCredentials: { url: process.env.DATABASE_URL }\n};\n\n# comandos típicos del flujo\nnpx drizzle-kit generate\nnpx drizzle-kit migrate',preview('Migraciones','<p style="margin:0">schema.ts/js → drizzle-kit → SQL versionado → PostgreSQL.</p>'),'Antes de aplicar un cambio destructivo, prueba la migración con una copia de datos representativa.',{
        guide:[['Crear','drizzle.config.js','Configura origen y salida.'],['Generar','drizzle/<migration>.sql','Versiona el SQL generado.']],filesToCreate:[file('drizzle.config.js','Configuración.'),file('drizzle/<migration>.sql','Migración generada.','DRIZZLE KIT','npx drizzle-kit generate')]
      }),
      O('decision','12. Cuándo elegir cada uno','Prisma puede convenir cuando priorizas una experiencia guiada, schema central y cliente fuertemente tipado. Sequelize encaja bien en equipos que prefieren un ORM clásico con modelos y asociaciones. Drizzle es atractivo cuando quieres TypeScript y conservar una relación muy visible con SQL. En todos los casos compara soporte del motor, runtime, migraciones, consultas avanzadas, edge/serverless y experiencia del equipo.','Decide con preguntas:\n1. ¿El equipo domina SQL?\n2. ¿Necesita cliente generado?\n3. ¿Prefiere modelos clásicos?\n4. ¿Usará serverless/edge?\n5. ¿Qué motores necesita?\n6. ¿Cómo hará migraciones?\n7. ¿Necesita SQL raw frecuente?\n8. ¿Cómo probará transacciones?',preview('Regla práctica','<p style="margin:0">Prisma → DX guiada · Sequelize → ORM clásico · Drizzle → tipado y SQL visible.</p>'),'Haz una prueba pequeña con una consulta real del proyecto antes de comprometer toda la arquitectura.',{
        guide:[['Crear','docs/orm-decision.md','Registra criterios, prueba y decisión.']],filesToCreate:[file('docs/orm-decision.md','ADR de elección del ORM.')]
      }),
      O('project','13. Proyecto · usuarios y tareas con PostgreSQL','Construye la misma API con un solo ORM elegido. Debes crear User y Task, relación 1:N, migración inicial, repositorio de datos y una transacción que cree un usuario junto con su primera tarea. Añade una consulta de tareas pendientes por usuario y una prueba.','orm-project/\n├── src/\n│   ├── db/\n│   ├── repositories/\n│   └── server.js\n├── migrations/ o prisma/ o drizzle/\n├── tests/\n├── .env.example\n└── package.json\n\nEndpoints sugeridos:\nPOST /users\nPOST /users/:id/tasks\nGET  /users/:id/tasks?done=false',preview('Resultado','<p style="margin:0">API → repository → ORM → PostgreSQL; migraciones reproducibles y pruebas.</p>'),'No mezcles consultas del ORM directamente por toda la aplicación; centralízalas para facilitar pruebas y cambios.',{
        guide:[['Crear','orm-project/src/db/','Conexión y esquema.'],['Crear','orm-project/src/repositories/','Consultas.'],['Crear','orm-project/tests/','Pruebas.'],['Crear','orm-project/.env.example','Variables.']],filesToCreate:[file('orm-project/src/server.js','API.'),file('orm-project/src/db/client.js','Conexión.'),file('orm-project/src/repositories/tasks.js','Acceso a datos.'),file('orm-project/tests/tasks.test.js','Pruebas.'),file('orm-project/.env.example','DATABASE_URL sin secreto.')]
      })
    ]
  });
})();
