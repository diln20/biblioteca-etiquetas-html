(()=>{
  if(window.__frontendFrameworksAdded)return;
  window.__frontendFrameworksAdded=true;

  const result=(color,title,text)=>`<article style="max-width:460px;padding:22px;border:1px solid #cbd5e1;border-radius:14px;font-family:system-ui;background:#fff"><h2 style="margin-top:0;color:${color}">${title}</h2><p style="color:#334155">${text}</p><button style="padding:9px 14px;border:0;border-radius:8px;color:#fff;background:${color}">Contador: 1</button></article>`;
  const F=(topic,name,description,code,preview,tip)=>T(topic,name,description,code,preview,[],{kind:'Framework frontend',tip});
  const add=(title,description,quote,challenge,items)=>sections.push({title,description,quote,challenge,items});

  add('Frameworks frontend · Introducción','Conoce qué problema resuelven React, Angular, Vue y Svelte, qué conocimientos necesitas y cómo elegir una ruta de aprendizaje.','“Los frameworks organizan componentes, estado y eventos; el navegador sigue mostrando HTML, CSS y DOM.”','Compara las cuatro opciones y elige una para construir el mismo contador.',[
    F('Concepto','¿Qué es un framework frontend?','Una interfaz tradicional puede crecer hasta mezclar estructura, eventos y datos en muchos archivos. Las herramientas frontend permiten dividirla en componentes, mantener un estado y actualizar el DOM cuando cambia ese estado. React es una biblioteca de interfaz; Angular, Vue y Svelte se presentan como frameworks o ecosistemas con distinto alcance.','HTML + CSS + JavaScript\n        ↓\nComponente + estado + eventos\n        ↓\nDOM actualizado en el navegador','<p style="font-family:system-ui"><strong>Idea principal:</strong> los componentes organizan la interfaz; no reemplazan los fundamentos web.</p>','Estudia primero HTML semántico, CSS, JavaScript moderno, módulos, eventos y DOM.'),
    F('Requisitos','Preparar el entorno','Para trabajar localmente necesitas Node.js, npm y un editor. Node ejecuta las herramientas de desarrollo; npm instala dependencias definidas en package.json; el servidor local transforma los archivos y recarga el navegador mientras programas. Comprueba las versiones antes de crear un proyecto.','node --version\nnpm --version\n\n# Cada framework tendrá después su propio comando de creación.','<pre style="padding:16px;border-radius:10px;background:#0f172a;color:#a7f3d0">node --version\nnpm --version</pre>','Usa una versión de Node compatible con la documentación actual del framework elegido.'),
    F('Comparación','¿Cuál elegir?','React destaca por su ecosistema flexible y JSX; Angular ofrece una plataforma estructurada con TypeScript; Vue combina plantillas cercanas a HTML con adopción gradual; Svelte compila componentes escritos con HTML, CSS y JavaScript. Para aprender, elige una sola opción y crea proyectos pequeños antes de comparar arquitecturas avanzadas.','React  → JSX, Hooks y ecosistema flexible\nAngular→ TypeScript, CLI y plataforma completa\nVue    → plantillas, directivas y adopción gradual\nSvelte → compilador, runes y sintaxis compacta','<table style="width:100%;border-collapse:collapse;font-family:system-ui"><tr><th>React</th><th>Angular</th><th>Vue</th><th>Svelte</th></tr><tr><td>JSX</td><td>Plantillas</td><td>Plantillas</td><td>.svelte</td></tr></table>','No instales los cuatro en un mismo proyecto de práctica; repite la misma aplicación por separado.'),
    F('Ruta','Orden de aprendizaje','En cualquiera de las cuatro rutas conviene seguir el mismo orden: crear el proyecto, reconocer los archivos, construir un componente, pasar datos, manejar estado y eventos, renderizar listas y condicionales, y terminar con una práctica integrada. Esta biblioteca organiza cada framework siguiendo exactamente esa progresión.','1. Instalación\n2. Estructura\n3. Componente\n4. Datos o propiedades\n5. Estado y eventos\n6. Listas y condicionales\n7. Práctica integrada','<ol style="font-family:system-ui;line-height:1.8"><li>Instalar</li><li>Comprender</li><li>Construir</li><li>Practicar</li></ol>','Completa la práctica de un framework antes de pasar al siguiente.')
  ]);

  add('Frameworks frontend · React','Aprende React progresivamente: instalación con Vite, JSX, componentes, props, estado, eventos y renderizado de colecciones.','“En React, una interfaz se expresa como componentes que producen JSX a partir de datos.”','Construye un contador reutilizable y una lista de tareas con React.',[
    F('Instalación','Crear un proyecto con React','Vite crea un proyecto pequeño con el compilador, servidor de desarrollo y soporte para JSX. El comando genera la carpeta mi-react; después debes instalar las dependencias, iniciar el servidor y abrir la dirección local que muestra la terminal. Create React App ya no es la opción recomendada para proyectos nuevos.','npm create vite@latest mi-react -- --template react\ncd mi-react\nnpm install\nnpm run dev','<pre style="padding:16px;border-radius:10px;background:#0f172a;color:#7dd3fc">VITE listo\nLocal: http://localhost:5173/</pre>','Ejecuta los comandos en una carpeta donde quieras crear el proyecto, no dentro de esta biblioteca.'),
    F('JSX','Primer componente','Un componente React es una función cuyo nombre comienza con mayúscula y devuelve JSX. JSX permite escribir una estructura parecida a HTML dentro de JavaScript, pero usa className para las clases y llaves para insertar expresiones. App suele ser el componente raíz desde el que se combinan los demás.','export default function App() {\n  const curso = "React";\n  return (\n    <main className="app">\n      <h1>Aprendiendo {curso}</h1>\n    </main>\n  );\n}',result('#087ea4','React','El componente App convierte JSX y datos en elementos del DOM.'),'Devuelve un solo elemento raíz o usa un fragmento <>...</>.'),
    F('Props','Comunicación entre componentes','Las props son valores que un componente padre entrega a un componente hijo. El hijo las recibe como un objeto, puede mostrarlas o utilizarlas para decidir su presentación, pero no debe modificarlas. Este flujo unidireccional hace más fácil entender de dónde viene cada dato.','function Saludo({ nombre }) {\n  return <p>Hola, {nombre}</p>;\n}\n\nexport default function App() {\n  return <Saludo nombre="Ana" />;\n}',result('#087ea4','React','Hola, Ana'),'Usa props para configurar componentes reutilizables y estado para datos que cambian.'),
    F('Estado y eventos','useState y onClick','useState conserva información entre renderizados y devuelve el valor actual junto con una función para actualizarlo. Al hacer clic, onClick ejecuta el manejador; setConteo cambia el estado y React vuelve a renderizar el componente con el nuevo valor visible.','import { useState } from "react";\n\nexport default function Contador() {\n  const [conteo, setConteo] = useState(0);\n  return <button onClick={() => setConteo(c => c + 1)}>\n    Contador: {conteo}\n  </button>;\n}',result('#087ea4','React','El clic actualiza el estado y provoca un nuevo render.'),'No modifiques el estado directamente; utiliza siempre su función actualizadora.'),
    F('Renderizado','Condicionales y listas','React usa JavaScript para decidir qué mostrar. El operador && puede renderizar contenido condicional y map transforma una colección en elementos JSX. Cada elemento creado por map necesita una key estable para que React identifique correctamente las altas, bajas y cambios.','const tareas = [{ id: 1, texto: "Estudiar JSX" }];\n\nexport default function Lista() {\n  return <section>\n    {tareas.length === 0 && <p>Sin tareas</p>}\n    <ul>{tareas.map(tarea =>\n      <li key={tarea.id}>{tarea.texto}</li>\n    )}</ul>\n  </section>;\n}','<section style="font-family:system-ui"><h2>Tareas</h2><ul><li>Estudiar JSX</li></ul></section>','Evita usar la posición del arreglo como key si los elementos pueden cambiar de orden.'),
    F('Práctica','Proyecto React: lista de tareas','Integra componentes, props, useState, eventos y listas en una aplicación de tareas. Empieza con App y separa TareaItem solamente cuando la representación de una tarea tenga responsabilidad propia. Añade primero la creación y después el cambio de estado completada.','1. Crea el proyecto con Vite.\n2. Diseña el formulario y la lista en App.\n3. Guarda tareas con useState.\n4. Agrega tareas con onSubmit.\n5. Renderiza con map y una key estable.\n6. Crea Tarea para mostrar texto y estado.\n7. Añade completar y eliminar.','<div style="font-family:system-ui"><h2>Mis tareas</h2><input placeholder="Nueva tarea"><button>Agregar</button><ul><li>Aprender componentes ✓</li></ul></div>','Comprueba que agregar, completar y eliminar actualicen la vista sin recargar la página.')
  ]);

  add('Frameworks frontend · Angular','Recorre Angular desde Angular CLI hasta componentes, plantillas, entradas, señales, eventos y directivas de control.','“Angular organiza una aplicación alrededor de componentes TypeScript y plantillas declarativas.”','Construye un catálogo pequeño con componentes standalone de Angular.',[
    F('Instalación','Crear un proyecto Angular','Angular CLI crea el workspace, instala los paquetes y prepara compilación, pruebas y servidor local. ng new solicita opciones como estilos y enrutamiento; para una primera práctica puedes aceptar los valores predeterminados. ng serve --open compila, observa cambios y abre la aplicación.','npm install -g @angular/cli\nng new mi-angular\ncd mi-angular\nng serve --open','<pre style="padding:16px;border-radius:10px;background:#0f172a;color:#fda4af">Angular listo\nhttp://localhost:4200/</pre>','Comprueba primero que tu versión de Node sea compatible con la versión actual de Angular.'),
    F('@Component','Componente y plantilla','Un componente standalone relaciona una clase TypeScript con un selector, una plantilla y estilos. La clase contiene datos y comportamiento; template describe el HTML que se mostrará; selector permite insertar el componente desde otra plantilla como si fuera una etiqueta personalizada.','import { Component } from "@angular/core";\n\n@Component({\n  selector: "app-saludo",\n  standalone: true,\n  template: `<h2>Hola, {{ nombre }}</h2>`\n})\nexport class SaludoComponent {\n  nombre = "Ana";\n}','<h2 style="font-family:system-ui;color:#dd0031">Hola, Ana</h2>','Mantén la plantilla pequeña; usa templateUrl cuando el marcado necesite un archivo separado.'),
    F('Inputs','Datos de padre a hijo','input declara un dato que el componente padre puede proporcionar mediante property binding. El hijo usa ese valor en su plantilla, pero el padre continúa siendo su propietario. Esto permite reutilizar la misma tarjeta con nombres, precios o estados diferentes.','import { Component, input } from "@angular/core";\n\n@Component({\n  selector: "app-producto",\n  standalone: true,\n  template: `<h3>{{ nombre() }}</h3>`\n})\nexport class ProductoComponent {\n  nombre = input.required<string>();\n}\n\n// Uso: <app-producto [nombre]="titulo" />','<article style="font-family:system-ui"><h3>Curso de Angular</h3></article>','Los corchetes [propiedad] evalúan una expresión; sin corchetes se entrega texto literal.'),
    F('Estado y eventos','Señales y eventos de plantilla','Una signal conserva estado reactivo. La plantilla lee su valor llamándola como función y un evento entre paréntesis ejecuta código de la clase. update calcula el siguiente valor usando el anterior; Angular detecta el cambio y actualiza el texto que depende de la señal.','import { Component, signal } from "@angular/core";\n\n@Component({\n  selector: "app-contador",\n  standalone: true,\n  template: `<button (click)="incrementar()">\n    Contador: {{ conteo() }}\n  </button>`\n})\nexport class ContadorComponent {\n  conteo = signal(0);\n  incrementar() { this.conteo.update(n => n + 1); }\n}',result('#dd0031','Angular','La señal cambia y la plantilla se actualiza.'),'Usa set para reemplazar el valor y update cuando el nuevo valor depende del anterior.'),
    F('Plantillas','Condicionales y listas','El control de flujo de plantillas decide qué elementos existen. @if muestra un bloque cuando la expresión es verdadera y @for repite el marcado por cada elemento. track indica cómo identificar cada registro y ayuda a actualizar la lista de forma eficiente.','@if (productos.length === 0) {\n  <p>No hay productos.</p>\n} @else {\n  <ul>\n    @for (producto of productos; track producto.id) {\n      <li>{{ producto.nombre }}</li>\n    }\n  </ul>\n}','<section style="font-family:system-ui"><h2>Productos</h2><ul><li>Curso de Angular</li></ul></section>','Selecciona una propiedad única y estable para track, normalmente el identificador del registro.'),
    F('Práctica','Proyecto Angular: catálogo','Construye un catálogo para unir componentes standalone, entradas, señales, eventos y bloques @if y @for. El componente principal conserva la colección; ProductoCard recibe cada producto; un filtro reactivo decide qué registros se muestran.','1. Crea el proyecto con Angular CLI.\n2. Genera ProductoCard.\n3. Define productos en el componente principal.\n4. Pasa cada producto mediante input.\n5. Renderiza la colección con @for.\n6. Añade una signal para el filtro.\n7. Muestra el estado vacío con @if.','<div style="font-family:system-ui"><h2>Catálogo</h2><input placeholder="Filtrar"><article>Curso Angular · Disponible</article></div>','Prueba el catálogo vacío, con todos los registros y con un filtro sin coincidencias.')
  ]);

  add('Frameworks frontend · Vue','Aprende Vue 3 con create-vue, componentes de archivo único, props, refs, eventos, directivas y una práctica integrada.','“Vue amplía las plantillas HTML con reactividad y componentes de adopción progresiva.”','Construye una lista filtrable usando Composition API y script setup.',[
    F('Instalación','Crear un proyecto Vue','create-vue es la herramienta oficial para generar una aplicación basada en Vite. El asistente pregunta por TypeScript, Router, Pinia y pruebas; un principiante puede elegir No y añadirlos cuando exista una necesidad. Después se instalan las dependencias y se inicia el servidor local.','npm create vue@latest\ncd mi-vue\nnpm install\nnpm run dev','<pre style="padding:16px;border-radius:10px;background:#0f172a;color:#86efac">Vue listo\nLocal: http://localhost:5173/</pre>','Escribe mi-vue cuando el asistente solicite el nombre del proyecto.'),
    F('SFC','Primer componente .vue','Un Single-File Component reúne lógica, plantilla y estilos en un archivo .vue. script setup contiene el JavaScript del componente; template define el marcado; style scoped limita los estilos a ese componente. La interpolación con dobles llaves muestra expresiones en el HTML.','<!-- Saludo.vue -->\n<script setup>\nconst nombre = "Ana";\n</script>\n\n<template>\n  <h2 class="titulo">Hola, {{ nombre }}</h2>\n</template>\n\n<style scoped>\n.titulo { color: #42b883; }\n</style>','<h2 style="font-family:system-ui;color:#42b883">Hola, Ana</h2>','En template usa atributos HTML normales como class; Vue los procesa durante el render.'),
    F('Props','Comunicación entre componentes','defineProps declara los datos que un componente espera recibir. El padre enlaza un valor con :nombre, forma abreviada de v-bind:nombre. La prop puede utilizarse directamente en la plantilla y debe tratarse como un dato de solo lectura dentro del hijo.','<!-- Saludo.vue -->\n<script setup>\ndefineProps({ nombre: String });\n</script>\n<template><p>Hola, {{ nombre }}</p></template>\n\n<!-- App.vue -->\n<Saludo :nombre="usuario.nombre" />','<p style="font-family:system-ui">Hola, Ana</p>','Declara tipos y valores requeridos para detectar usos incorrectos durante el desarrollo.'),
    F('Reactividad','ref y eventos','ref crea una referencia reactiva. En JavaScript se accede y modifica mediante .value; dentro de la plantilla Vue la desenvuelve automáticamente. @click es la abreviatura de v-on:click y conecta el botón con una expresión o función que actualiza el estado.','<script setup>\nimport { ref } from "vue";\nconst conteo = ref(0);\n</script>\n\n<template>\n  <button @click="conteo++">\n    Contador: {{ conteo }}\n  </button>\n</template>',result('#42b883','Vue','ref mantiene el estado y @click lo actualiza.'),'Recuerda .value en script; en template normalmente no es necesario.'),
    F('Directivas','Condicionales y listas','v-if crea o elimina un bloque según una condición; v-else presenta la alternativa. v-for repite un elemento y :key asigna una identidad estable a cada registro. Estas directivas conservan la plantilla cerca del HTML y expresan claramente qué depende de los datos.','<p v-if="tareas.length === 0">Sin tareas</p>\n<ul v-else>\n  <li v-for="tarea in tareas" :key="tarea.id">\n    {{ tarea.texto }}\n  </li>\n</ul>','<section style="font-family:system-ui"><h2>Tareas</h2><ul><li>Aprender directivas</li></ul></section>','No combines v-if y v-for en el mismo elemento; filtra la colección antes o usa un contenedor.'),
    F('Práctica','Proyecto Vue: lista filtrable','Integra componentes, props, ref, eventos y directivas en una lista filtrable. App conserva los datos y el texto de búsqueda; TareaItem recibe una tarea. Una propiedad calculada puede derivar los elementos visibles sin duplicar el estado original.','1. Crea el proyecto con create-vue.\n2. Diseña App y TareaItem.\n3. Guarda búsqueda y tareas con ref.\n4. Enlaza el campo con v-model.\n5. Filtra con computed.\n6. Repite resultados con v-for y :key.\n7. Muestra el estado vacío con v-if.','<div style="font-family:system-ui"><h2>Buscar tareas</h2><input value="Vue"><ul><li>Practicar Vue</li></ul></div>','Comprueba la lista completa, una búsqueda con resultados y otra sin coincidencias.')
  ]);

  add('Frameworks frontend · Svelte','Descubre Svelte 5 y SvelteKit mediante instalación, archivos .svelte, propiedades, runes, eventos y bloques de plantilla.','“Svelte compila componentes declarativos en JavaScript optimizado para el navegador.”','Construye una lista de notas con estado reactivo y componentes Svelte.',[
    F('Instalación','Crear un proyecto Svelte','La CLI sv crea actualmente un proyecto SvelteKit. El asistente permite escoger una plantilla, tipos y complementos; para comenzar basta una plantilla mínima. Tras instalar las dependencias, npm run dev inicia el entorno local y -- --open solicita abrirlo en el navegador.','npx sv create mi-svelte\ncd mi-svelte\nnpm install\nnpm run dev -- --open','<pre style="padding:16px;border-radius:10px;background:#0f172a;color:#fdba74">SvelteKit listo\nLocal: http://localhost:5173/</pre>','Elige minimal y evita complementos hasta necesitarlos en la práctica.'),
    F('.svelte','Primer componente','Un archivo .svelte puede contener script, marcado y style. Las variables declaradas en script se insertan con llaves y los estilos del componente se aíslan automáticamente. El compilador transforma esta definición en JavaScript que crea y actualiza los elementos del navegador.','<!-- Saludo.svelte -->\n<script>\n  let nombre = "Ana";\n</script>\n\n<h2>Hola, {nombre}</h2>\n\n<style>\n  h2 { color: #ff3e00; }\n</style>','<h2 style="font-family:system-ui;color:#ff3e00">Hola, Ana</h2>','Mantén juntos marcado, comportamiento y estilos cuando pertenecen a una sola responsabilidad.'),
    F('$props','Comunicación entre componentes','$props recibe los datos enviados por el componente padre. La desestructuración documenta qué propiedades usa el hijo y permite establecer valores predeterminados. El padre asigna atributos al componente con una sintaxis cercana a la de una etiqueta HTML.','<!-- Saludo.svelte -->\n<script>\n  let { nombre = "visitante" } = $props();\n</script>\n<p>Hola, {nombre}</p>\n\n<!-- Uso -->\n<Saludo nombre="Ana" />','<p style="font-family:system-ui">Hola, Ana</p>','Una prop describe entrada del componente; no la uses como dueño de estado compartido sin definir ese contrato.'),
    F('$state','Estado y eventos','La rune $state crea estado reactivo. El marcado lee la variable entre llaves y onclick recibe el manejador del evento. Cuando el manejador incrementa conteo, Svelte actualiza el texto dependiente sin que tengas que localizar ni modificar el elemento manualmente.','<script>\n  let conteo = $state(0);\n</script>\n\n<button onclick={() => conteo++}>\n  Contador: {conteo}\n</button>',result('#ff3e00','Svelte','El evento cambia $state y el texto se actualiza.'),'Usa $derived para valores calculados a partir del estado en lugar de mantener copias sincronizadas.'),
    F('Bloques','Condicionales y listas','Los bloques {#if} y {#each} controlan qué marcado produce el componente. if permite mostrar estados alternativos; each recorre una colección. La expresión entre paréntesis al final de each funciona como clave estable para identificar cada registro.','{#if tareas.length === 0}\n  <p>Sin tareas</p>\n{:else}\n  <ul>\n    {#each tareas as tarea (tarea.id)}\n      <li>{tarea.texto}</li>\n    {/each}\n  </ul>\n{/if}','<section style="font-family:system-ui"><h2>Tareas</h2><ul><li>Aprender bloques</li></ul></section>','Utiliza una clave única si la lista puede reordenarse, eliminar elementos o insertar nuevos.'),
    F('Práctica','Proyecto Svelte: notas','Combina componentes, $props, $state, eventos y bloques en una aplicación de notas. La página conserva la colección y Nota recibe cada registro. Empieza con agregar y eliminar; después deriva el número de notas sin duplicar manualmente ese valor.','1. Crea el proyecto con sv.\n2. Diseña la página y el componente Nota.\n3. Guarda las notas con $state.\n4. Conecta el formulario con onsubmit.\n5. Renderiza con {#each} y una clave.\n6. Muestra el estado vacío con {#if}.\n7. Calcula el total con $derived.','<div style="font-family:system-ui"><h2>Mis notas (1)</h2><input placeholder="Nueva nota"><button>Guardar</button><article>Practicar Svelte</article></div>','Comprueba agregar, eliminar, estado vacío y actualización automática del total.')
  ]);

  const intro=sections.find(section=>section.title==='Frameworks frontend · Introducción');
  intro.items.push(
    F('Arquitectura','SPA, SSR y aplicaciones híbridas','Una SPA actualiza vistas en el navegador después de una carga inicial; SSR genera HTML en el servidor para cada solicitud; la generación estática prepara páginas durante el build; un enfoque híbrido combina estrategias por ruta. La elección afecta rendimiento inicial, SEO, infraestructura y complejidad.','SPA → render principal en el navegador\nSSR → HTML generado por solicitud\nSSG → HTML generado durante el build\nHíbrido → estrategia diferente por ruta','<div style="display:grid;grid-template-columns:repeat(2,1fr);gap:10px;font-family:system-ui"><div style="padding:14px;background:#e0f2fe">SPA / cliente</div><div style="padding:14px;background:#ecfdf5">SSR / servidor</div></div>','Empieza con una SPA sencilla; incorpora renderizado de servidor cuando exista una necesidad medible.'),
    F('Estructura','Organizar un proyecto','Una estructura útil separa componentes visuales, páginas o rutas, acceso a datos, estilos y recursos. La carpeta exacta cambia entre tecnologías, pero la regla es estable: agrupa archivos que cambian juntos y evita carpetas genéricas llenas de elementos sin relación.','src/\n├─ components/\n├─ pages/\n├─ services/\n├─ styles/\n├─ assets/\n└─ main.js','<pre style="padding:16px;border-radius:10px;background:#0f172a;color:#cbd5e1">components · pages · services · styles</pre>','No crees capas vacías para un proyecto pequeño; extrae una carpeta cuando ya tenga contenido relacionado.'),
    F('Accesibilidad','Accesibilidad en componentes','Un framework no vuelve accesible una interfaz automáticamente. Conserva HTML semántico, etiquetas asociadas a formularios, orden lógico del foco, controles de teclado, mensajes de error comprensibles y estados ARIA sincronizados. Prueba el resultado renderizado, no solamente el código del componente.','<label for="correo">Correo</label>\n<input id="correo" type="email" aria-describedby="error-correo">\n<p id="error-correo" role="alert">Escribe un correo válido.</p>','<form style="font-family:system-ui"><label for="demo-correo">Correo</label><br><input id="demo-correo" type="email"><p style="color:#b91c1c">Escribe un correo válido.</p></form>','Prefiere elementos nativos antes de recrear botones, enlaces o cuadros de diálogo con div.'),
    F('Producción','Build, pruebas y despliegue','El servidor de desarrollo prioriza recarga rápida y mensajes de depuración; no debe publicarse como servidor de producción. El comando build genera recursos optimizados. Antes de desplegar, ejecuta pruebas, revisa variables de entorno, rutas, tamaños del bundle y una vista previa de la salida final.','npm run test\nnpm run build\nnpm run preview\n\n# Publicar la carpeta generada por la herramienta','<pre style="padding:16px;border-radius:10px;background:#0f172a;color:#a7f3d0">test ✓  build ✓  preview ✓</pre>','Nunca incluyas secretos en variables que terminan dentro del JavaScript enviado al navegador.')
  );

  const beforePractice=(level,items)=>{
    const section=sections.find(entry=>entry.title===`Frameworks frontend · ${level}`);
    section.items.splice(section.items.length-1,0,...items);
  };

  beforePractice('React',[
    F('Formularios','Inputs controlados','Un input controlado recibe su value desde el estado y comunica cada cambio mediante onChange. Este patrón mantiene una fuente de verdad para validar, transformar o enviar el valor. El formulario escucha onSubmit y evita la navegación predeterminada cuando procesa los datos en JavaScript.','import { useState } from "react";\n\nfunction Formulario() {\n  const [nombre, setNombre] = useState("");\n  function enviar(evento) {\n    evento.preventDefault();\n    console.log(nombre);\n  }\n  return <form onSubmit={enviar}>\n    <label>Nombre <input value={nombre} onChange={e => setNombre(e.target.value)} /></label>\n    <button>Guardar</button>\n  </form>;\n}','<form style="font-family:system-ui"><label>Nombre <input value="Ana"></label> <button>Guardar</button></form>','Conserva validaciones HTML como required y type aunque también valides en JavaScript.'),
    F('Estado compartido','Elevar estado y Context','Cuando dos componentes necesitan el mismo dato, mueve el estado al ancestro común y distribuye valor y acciones mediante props. Context evita pasar una prop por muchos niveles para información transversal como tema o sesión; no es necesario para datos usados por pocos componentes cercanos.','const Tema = createContext("claro");\n\nfunction App() {\n  return <Tema.Provider value="oscuro"><Panel /></Tema.Provider>;\n}\nfunction Boton() {\n  const tema = useContext(Tema);\n  return <button className={tema}>Guardar</button>;\n}','<button style="padding:10px 16px;background:#172033;color:white;border:0;border-radius:8px">Guardar</button>','Eleva el estado antes de añadir una biblioteca global; suele ser suficiente para interfaces pequeñas.'),
    F('Efectos','Sincronización con sistemas externos','useEffect sincroniza un componente con algo externo a React, como una conexión, temporizador o API del navegador. El cuerpo configura la sincronización y puede devolver una limpieza. No uses Effects para calcular datos que pueden derivarse directamente durante el render.','useEffect(() => {\n  const id = setInterval(() => setHora(new Date()), 1000);\n  return () => clearInterval(id);\n}, []);','<p style="font-family:system-ui">Reloj actualizado: 10:30:15</p>','Incluye todas las dependencias reactivas y limpia suscripciones, eventos o temporizadores.'),
    F('Datos','Cargar datos y representar estados','Una petición necesita al menos estados de carga, éxito, vacío y error. Puedes encapsular fetch dentro de una capa de datos o usar las capacidades del framework de aplicación elegido. Si haces la petición en un Effect, cancela o ignora respuestas obsoletas para evitar actualizar con datos antiguos.','const [estado, setEstado] = useState({ loading: true, data: [], error: null });\n\n// Render\nif (estado.loading) return <p>Cargando…</p>;\nif (estado.error) return <p role="alert">No se pudo cargar.</p>;\nreturn <Lista datos={estado.data} />;','<section style="font-family:system-ui"><p>Carga finalizada</p><ul><li>Curso React</li></ul></section>','Diseña los cuatro estados antes de conectar la API real.'),
    F('Reducers y refs','useReducer y useRef','useReducer reúne transiciones complejas de estado en una función pura que recibe estado y acción. useRef conserva un valor sin provocar render y también referencia nodos del DOM para tareas como enfocar un input. Ninguno reemplaza el estado visible normal.','const [estado, dispatch] = useReducer(reducer, { total: 0 });\nconst inputRef = useRef(null);\n\n<button onClick={() => dispatch({ type: "sumar" })}>+</button>\n<button onClick={() => inputRef.current.focus()}>Enfocar</button>','<div style="font-family:system-ui"><button>+</button> <button>Enfocar</button><p>Total: 1</p></div>','Usa reducer cuando varias acciones modifican un estado relacionado; no para un único booleano.'),
    F('Reutilización','Hooks personalizados y pruebas','Un Hook personalizado extrae lógica reactiva reutilizable y su nombre comienza con use. Cada llamada conserva su propio estado; comparte lógica, no una instancia de estado. Prueba el comportamiento visible del componente y las interacciones importantes, evitando acoplar las pruebas a detalles internos.','function useContador(inicial = 0) {\n  const [valor, setValor] = useState(inicial);\n  const incrementar = () => setValor(v => v + 1);\n  return { valor, incrementar };\n}\n\n// En una prueba: renderizar, hacer clic y comprobar el texto.','<div style="font-family:system-ui"><button>Incrementar</button><p>Valor esperado después del clic: 1</p></div>','Extrae un Hook cuando la intención se repite o cuando encapsula una sincronización compleja.')
  ]);

  beforePractice('Angular',[
    F('Outputs','Eventos de hijo a padre','output permite que un componente hijo comunique una acción sin conocer la implementación del padre. El hijo emite un dato y el padre escucha el evento entre paréntesis. Este contrato complementa input: datos hacia abajo y eventos hacia arriba.','export class ProductoCard {\n  seleccionar = output<number>();\n}\n// plantilla hija\n<button (click)="seleccionar.emit(producto.id)">Elegir</button>\n\n// plantilla padre\n<app-producto (seleccionar)="abrir($event)" />','<button style="padding:10px 16px;background:#dd0031;color:white;border:0">Elegir producto</button>','Emite datos mínimos y claros; evita pasar el evento completo del navegador si el padre no lo necesita.'),
    F('Formularios','Formularios reactivos','Los formularios reactivos definen controles y validadores en TypeScript, proporcionan acceso síncrono al estado y escalan bien cuando hay reglas complejas. La plantilla conecta formGroup y formControlName, muestra errores y desactiva el envío mientras el modelo sea inválido.','formulario = new FormGroup({\n  correo: new FormControl("", [Validators.required, Validators.email])\n});\n\n<form [formGroup]="formulario" (ngSubmit)="guardar()">\n  <input type="email" formControlName="correo">\n  <button [disabled]="formulario.invalid">Guardar</button>\n</form>','<form style="font-family:system-ui"><input type="email" placeholder="correo@ejemplo.com"> <button>Guardar</button></form>','Mantén también atributos nativos apropiados y anuncia los errores de validación.'),
    F('Servicios','Servicios e inyección de dependencias','Un servicio encapsula lógica o acceso a datos que varios componentes necesitan. providedIn root ofrece una instancia compartida y inject solicita la dependencia donde Angular dispone de un contexto de inyección. El componente consume el contrato sin construir manualmente el servicio.','@Injectable({ providedIn: "root" })\nexport class CursosService {\n  listar() { return ["HTML", "CSS"]; }\n}\n\nexport class CursosPage {\n  private cursosService = inject(CursosService);\n  cursos = this.cursosService.listar();\n}','<ul style="font-family:system-ui"><li>HTML</li><li>CSS</li></ul>','Mantén en el componente la presentación y en el servicio la operación compartida o externa.'),
    F('HTTP','Consumir una API con HttpClient','HttpClient realiza solicitudes tipadas y se integra con inyección de dependencias. Es recomendable aislar el acceso a datos en un servicio y representar carga, error, vacío y éxito en la vista. Las herramientas async o toSignal ayudan a administrar la suscripción junto con el ciclo del componente.','@Injectable({ providedIn: "root" })\nexport class CursosApi {\n  private http = inject(HttpClient);\n  listar() { return this.http.get<Curso[]>("/api/cursos"); }\n}\n\n// Componente\ncursos$ = inject(CursosApi).listar();','<section style="font-family:system-ui"><p>Datos recibidos</p><article>Curso de Angular</article></section>','Configura el cliente según la versión de Angular y maneja los errores cerca de la operación.'),
    F('Routing','Rutas y navegación','Angular Router relaciona rutas con componentes. router-outlet marca dónde aparece la vista activa y routerLink navega sin recargar el documento completo. Las rutas pueden incluir parámetros, hijos, carga diferida y guards, pero empieza con un mapa pequeño y una página no encontrada.','export const routes: Routes = [\n  { path: "", component: InicioPage },\n  { path: "cursos/:id", component: CursoPage },\n  { path: "**", component: NoEncontradoPage }\n];\n\n<a routerLink="/cursos/1">Ver curso</a>\n<router-outlet />','<nav style="font-family:system-ui"><a href="#">Inicio</a> · <a href="#">Curso 1</a></nav>','Un guard mejora la navegación, pero la autorización real siempre debe validarse en el servidor.'),
    F('Pruebas','Probar componentes y servicios','Una prueba de componente crea el entorno, establece entradas, ejecuta una interacción y verifica el DOM o un evento observable. Los servicios HTTP deben probarse con utilidades que controlen las solicitudes sin depender de la red real. Prioriza flujos críticos y contratos públicos.','it("incrementa al hacer clic", () => {\n  const fixture = TestBed.createComponent(ContadorComponent);\n  fixture.nativeElement.querySelector("button").click();\n  fixture.detectChanges();\n  expect(fixture.nativeElement.textContent).toContain("1");\n});','<pre style="padding:16px;background:#0f172a;color:#a7f3d0">1 prueba superada ✓</pre>','No pruebes detalles privados de la clase cuando puedes comprobar la conducta visible.')
  ]);

  beforePractice('Vue',[
    F('Eventos','Emits de hijo a padre','defineEmits declara los eventos que un componente puede emitir. El hijo comunica una intención y el padre decide cómo modificar el estado. Esto mantiene al componente reutilizable: no necesita conocer la colección, la ruta ni la API del padre.','<!-- TareaItem.vue -->\n<script setup>\nconst emit = defineEmits(["completar"]);\n</script>\n<button @click="emit(\'completar\')">Completar</button>\n\n<!-- Padre -->\n<TareaItem @completar="marcar(tarea.id)" />','<button style="padding:10px 16px;background:#42b883;color:white;border:0">Completar</button>','Declara nombres que describan la acción realizada, no el detalle interno del botón.'),
    F('Formularios','v-model y validación','v-model combina el valor de un control con la escucha de sus cambios. El estado reactivo recibe lo escrito y vuelve a alimentar la plantilla. Conserva label, type, required y mensajes comprensibles; el enlace de datos no reemplaza la semántica ni la validación.','<script setup>\nimport { ref } from "vue";\nconst correo = ref("");\n</script>\n\n<template>\n  <form @submit.prevent="guardar">\n    <label>Correo <input v-model.trim="correo" type="email" required></label>\n    <button>Guardar</button>\n  </form>\n</template>','<form style="font-family:system-ui"><label>Correo <input type="email"></label> <button>Guardar</button></form>','Usa modificadores como trim solo cuando representen realmente el formato esperado.'),
    F('Derivación','computed y watch','computed deriva un valor de otras fuentes reactivas y almacena el resultado mientras sus dependencias no cambien. watch ejecuta un efecto ante un cambio concreto, por ejemplo guardar una preferencia o iniciar una petición. No uses watch para mantener manualmente un valor que computed puede calcular.','const busqueda = ref("");\nconst visibles = computed(() =>\n  tareas.value.filter(t => t.texto.includes(busqueda.value))\n);\n\nwatch(busqueda, valor => localStorage.setItem("busqueda", valor));','<p style="font-family:system-ui">2 tareas coinciden con la búsqueda.</p>','Usa computed para valores; usa watch para efectos externos.'),
    F('Ciclo de vida','Carga y limpieza','onMounted ejecuta trabajo que necesita que el componente ya exista en el navegador. onUnmounted limpia listeners, temporizadores o conexiones. Para datos, modela carga, error y resultado, y evita dejar peticiones o callbacks que intenten modificar un componente destruido.','onMounted(() => window.addEventListener("resize", medir));\nonUnmounted(() => window.removeEventListener("resize", medir));\n\nconst estado = reactive({ cargando: true, datos: [], error: null });','<p style="font-family:system-ui">Ancho observado mientras el componente está montado.</p>','No uses un hook de montaje para cálculos que pueden hacerse directamente en setup.'),
    F('Reutilización','Composables','Un composable es una función cuyo nombre suele comenzar con use y que combina refs, computed, watchers y hooks para reutilizar una capacidad. Cada llamada crea su propio estado salvo que el estado se declare fuera. También sirve para separar una responsabilidad compleja de un componente grande.','export function useContador(inicial = 0) {\n  const valor = ref(inicial);\n  const incrementar = () => valor.value++;\n  return { valor, incrementar };\n}\n\nconst { valor, incrementar } = useContador();','<div style="font-family:system-ui"><button>Incrementar</button><p>Valor: 1</p></div>','Llama composables de forma síncrona dentro de setup y limpia los efectos que creen.'),
    F('Aplicación','Router, estado global y pruebas','Vue Router organiza vistas por URL y Pinia administra estado compartido cuando elevarlo ya no es práctico. Añade estas herramientas solamente si el proyecto las necesita. Prueba componentes mediante entradas, eventos y contenido renderizado; controla las dependencias externas.','// Responsabilidades\nrouter → URL y navegación\nstore  → estado compartido entre áreas\ncomponent → presentación e interacción local\ntest → contrato visible del componente','<div style="font-family:system-ui"><nav>Inicio · Cursos</nav><p>Estado compartido: sesión activa</p></div>','Empieza con props, emits y composables antes de convertir todo en estado global.')
  ]);

  beforePractice('Svelte',[
    F('Formularios','bind y formularios','bind:value mantiene sincronizado un control con una variable reactiva. onsubmit recibe el evento de envío y puede usar preventDefault para procesar localmente, aunque en SvelteKit las acciones de formulario permiten progresión mejorada y funcionamiento sin depender totalmente de JavaScript.','<script>\n  let nombre = $state("");\n  function guardar(evento) { evento.preventDefault(); }\n</script>\n<form onsubmit={guardar}>\n  <label>Nombre <input bind:value={nombre} required></label>\n  <button>Guardar</button>\n</form>','<form style="font-family:system-ui"><label>Nombre <input value="Ana"></label> <button>Guardar</button></form>','Conserva el envío nativo o las acciones del servidor cuando aporten resiliencia y accesibilidad.'),
    F('Derivación','$derived y $effect','$derived calcula valores a partir de estado reactivo sin mantener copias manuales. $effect sincroniza con sistemas externos después de que cambian sus dependencias y puede devolver limpieza. Igual que otros efectos, no debe usarse para una transformación que pertenece a $derived.','let precio = $state(100);\nlet cantidad = $state(2);\nlet total = $derived(precio * cantidad);\n\n$effect(() => {\n  document.title = `Total: ${total}`;\n});','<p style="font-family:system-ui">Total: $200</p>','Usa $derived para datos calculados y reserva $effect para el mundo externo.'),
    F('Componentes','Callbacks, snippets y composición','Las props pueden incluir callbacks para comunicar acciones al padre. Los snippets representan fragmentos de marcado reutilizable que un componente recibe y renderiza, permitiendo componer contenedores sin acoplarlos a un contenido fijo. Define contratos pequeños y explícitos.','<!-- Boton.svelte -->\n<script>let { children, onclick } = $props();</script>\n<button {onclick}>{@render children()}</button>\n\n<Boton onclick={() => guardar()}>Guardar</Boton>','<button style="padding:10px 16px;background:#ff3e00;color:white;border:0">Guardar</button>','Prefiere props directas para casos simples; usa snippets cuando realmente se entregue marcado.'),
    F('Ciclo de vida','Montaje, navegador y limpieza','onMount ejecuta una función cuando el componente aparece en el navegador y puede devolver una limpieza. Es apropiado para APIs exclusivas del DOM, widgets externos o suscripciones. En aplicaciones renderizadas en servidor, evita acceder a window o document durante la evaluación del módulo.','onMount(() => {\n  const id = setInterval(actualizar, 1000);\n  return () => clearInterval(id);\n});','<p style="font-family:system-ui">Suscripción activa mientras la vista está montada.</p>','Si el trabajo puede expresarse con estado y marcado, no necesita onMount.'),
    F('SvelteKit','Rutas, layouts y carga de datos','SvelteKit crea rutas según archivos dentro de src/routes. +page.svelte representa una página, +layout.svelte comparte estructura y una función load obtiene datos para una ruta. Mantén el acceso secreto en código exclusivo del servidor y devuelve a la página solamente la información necesaria.','src/routes/\n├─ +layout.svelte\n├─ +page.svelte\n└─ cursos/[id]/\n   ├─ +page.js\n   └─ +page.svelte\n\n// +page.js\nexport const load = ({ params }) => ({ id: params.id });','<nav style="font-family:system-ui">Inicio · Cursos · Detalle</nav>','Usa +page.server.js cuando la carga requiera credenciales o acceso directo a la base de datos.'),
    F('Experiencia','Transiciones y pruebas','Las transiciones se ejecutan cuando un elemento entra o sale debido a un cambio de estado. Respeta la preferencia de movimiento reducido y no dependas de la animación para comunicar información. En pruebas, verifica entradas, callbacks y DOM renderizado después de la interacción.','{#if visible}\n  <p transition:fade>Contenido disponible</p>\n{/if}\n\n// Prueba: renderizar, pulsar el botón y comprobar el texto.','<p style="font-family:system-ui;padding:14px;background:#fff7ed">Contenido disponible</p>','La interfaz debe seguir siendo comprensible con animaciones desactivadas.')
  ]);

  beforePractice('React',[
    F('Carpetas','Estructura recomendada de React','Vite crea los archivos mínimos; esta estructura añade carpetas solamente cuando el proyecto las necesita. components contiene piezas reutilizables, pages compone pantallas, services concentra acceso a APIs, hooks guarda lógica reactiva reutilizable y assets contiene recursos importados por el build. App.jsx conecta la aplicación y main.jsx la monta.','mi-react/\n├─ public/                 # archivos servidos sin transformar\n├─ src/\n│  ├─ assets/             # imágenes y fuentes importadas\n│  ├─ components/         # Button.jsx, CursoCard.jsx\n│  ├─ pages/              # InicioPage.jsx, CursoPage.jsx\n│  ├─ hooks/              # useCursos.js\n│  ├─ services/           # cursosApi.js\n│  ├─ styles/             # global.css\n│  ├─ App.jsx             # composición raíz\n│  └─ main.jsx            # createRoot\n├─ index.html\n├─ package.json\n└─ vite.config.js','<pre style="padding:16px;border-radius:10px;background:#0f172a;color:#7dd3fc">src/\n  components/\n  pages/\n  hooks/\n  services/\n  styles/</pre>','No crees una carpeta por tipo si solo tendrá un archivo; colócala cuando la separación ayude a navegar.')
  ]);
  beforePractice('Angular',[
    F('Carpetas','Estructura recomendada de Angular','Angular CLI crea la configuración del workspace, public, src, el punto main.ts y la carpeta app. Dentro de app conviene agrupar por funcionalidad: features contiene áreas de negocio, shared piezas reutilizables y core servicios globales. Cada componente puede mantener juntos su TypeScript, plantilla, estilos y prueba.','mi-angular/\n├─ public/\n├─ src/\n│  ├─ app/\n│  │  ├─ core/            # sesión, interceptores, servicios globales\n│  │  ├─ shared/          # componentes y utilidades reutilizables\n│  │  ├─ features/\n│  │  │  └─ cursos/       # páginas, componentes y servicios del área\n│  │  ├─ app.component.ts\n│  │  ├─ app.config.ts\n│  │  └─ app.routes.ts\n│  ├─ index.html\n│  ├─ main.ts\n│  └─ styles.css\n├─ angular.json\n├─ package.json\n└─ tsconfig.json','<pre style="padding:16px;border-radius:10px;background:#0f172a;color:#fda4af">src/app/\n  core/\n  shared/\n  features/\n  app.routes.ts</pre>','Agrupa por funcionalidad antes que crear carpetas globales enormes para components y services.')
  ]);
  beforePractice('Vue',[
    F('Carpetas','Estructura recomendada de Vue','create-vue genera un proyecto Vite y añade carpetas según las opciones seleccionadas. components almacena componentes reutilizables, views representa pantallas enlazadas por el router, composables contiene lógica reactiva, services acceso externo, stores estado compartido y assets recursos procesados. main.js crea y configura la aplicación.','mi-vue/\n├─ public/\n├─ src/\n│  ├─ assets/\n│  ├─ components/         # CursoCard.vue\n│  ├─ views/              # InicioView.vue\n│  ├─ composables/        # useCursos.js\n│  ├─ services/           # cursosApi.js\n│  ├─ stores/             # solo si se eligió Pinia\n│  ├─ router/             # solo si se eligió Vue Router\n│  ├─ App.vue\n│  └─ main.js\n├─ index.html\n├─ package.json\n└─ vite.config.js','<pre style="padding:16px;border-radius:10px;background:#0f172a;color:#86efac">src/\n  components/\n  views/\n  composables/\n  services/\n  router/</pre>','No añadas router o stores manualmente si la aplicación todavía no necesita esas responsabilidades.')
  ]);
  beforePractice('Svelte',[
    F('Carpetas','Estructura recomendada de SvelteKit','SvelteKit utiliza src/routes para crear rutas basadas en archivos y src/lib para código reutilizable importable mediante el alias $lib. static contiene recursos copiados sin transformación. Mantén componentes, acceso a datos y utilidades dentro de lib; coloca código secreto en archivos .server y pruebas fuera o junto a la funcionalidad según su alcance.','mi-svelte/\n├─ src/\n│  ├─ lib/\n│  │  ├─ components/     # CursoCard.svelte\n│  │  ├─ services/       # acceso reutilizable a datos\n│  │  └─ utils/\n│  ├─ routes/\n│  │  ├─ +layout.svelte\n│  │  ├─ +page.svelte\n│  │  └─ cursos/[id]/\n│  │     ├─ +page.server.js\n│  │     └─ +page.svelte\n│  ├─ app.html\n│  └─ app.css\n├─ static/\n├─ tests/\n├─ package.json\n├─ svelte.config.js\n└─ vite.config.js','<pre style="padding:16px;border-radius:10px;background:#0f172a;color:#fdba74">src/\n  lib/components/\n  lib/services/\n  routes/\nstatic/</pre>','Nunca importes código .server desde componentes que puedan ejecutarse en el navegador.')
  ]);

  buildNav();
  render();
})();
