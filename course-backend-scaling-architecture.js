(()=>{
  if(window.__backendScalingArchitectureAdded)return;
  window.__backendScalingArchitectureAdded=true;
  if(!Array.isArray(window.sections)||typeof window.T!=='function'||!window.CourseVisuals)return;
  const V=window.CourseVisuals;
  const item=(topic,name,description,code,preview,tip)=>T(topic,name,description,code,preview,[],{kind:'Arquitectura backend',tip});
  sections.push({title:'Backend APIs · Escalabilidad · 2. Distribuir trabajo',navLabel:'Escalabilidad · Distribuir trabajo',group:'Backend',primaryArea:'Backend',areaOrder:46,description:'Balanceador, varias instancias, caché y colas para reducir carga y aislar trabajos lentos.',quote:'“Cada pieza nueva debe responder a una métrica o un riesgo.”',challenge:'Diseña la evolución de una API stateless con balanceador, caché y workers.',items:[
    item('Horizontal','Varias instancias y balanceador','Cuando la aplicación es stateless, puedes ejecutar varias instancias detrás de un balanceador. Este distribuye solicitudes y retira instancias no saludables. Las sesiones deben usar tokens o almacenamiento compartido; las instancias deben ser reemplazables.','Internet\n  ↓\nLoad Balancer\n  ├─ API 1\n  ├─ API 2\n  └─ API 3\n        ↓\nBase de datos compartida\n\nHealth checks + despliegue gradual + autoscaling',V.diagram('USUARIOS\n   ▼\n┌──────────────────┐\n│ LOAD BALANCER    │\n└──────────────────┘\n   ├────► API 1\n   ├────► API 2\n   └────► API 3\n             ▼\n       BASE DE DATOS'),'Prueba qué ocurre cuando una instancia muere durante tráfico.'),
    item('Caché','Reducir trabajo repetido','Cache-aside consulta la caché; si no encuentra, lee la base y guarda con TTL. Define clave, duración, invalidación, tamaño y comportamiento cuando falla. Una caché incorrecta puede servir datos antiguos.','GET /products/42\n  ↓\n¿products:42 existe?\n  ├─ sí → responder\n  └─ no → DB → guardar TTL → responder\n\nDefinir: clave, TTL, invalidación y fallback',V.diagram('API\n  ├─ CACHE HIT ─────► respuesta rápida\n  └─ CACHE MISS\n        ▼\n     DATABASE\n        └─ guarda resultado con TTL'),'Mide hit rate; si casi todo es miss, la complejidad no compensa.'),
    item('Colas','Mover trabajo lento fuera de la petición','Correo, reportes o imágenes pueden procesarse de forma asíncrona. La API registra un trabajo y responde 202; un worker consume la cola. Diseña idempotencia, reintentos limitados y dead-letter queue.','Cliente → POST /reports\nAPI → job 123 → 202 Accepted\nCola → worker procesa\nWorker → guarda resultado\nCliente → GET /jobs/123\n\nEstados: queued, running, completed, failed',V.diagram('CLIENTE\n  ▼ petición corta\nAPI ─────► COLA ─────► WORKER\n │                         │\n └─ 202 + jobId            └─ resultado'),'El usuario necesita consultar estado o recibir una notificación.')
  ]});
})();
