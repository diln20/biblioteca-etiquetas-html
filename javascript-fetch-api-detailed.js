(()=>{
  if(window.__javascriptFetchDetailedAdded)return;
  window.__javascriptFetchDetailedAdded=true;
  if(!Array.isArray(window.sections)||typeof window.T!=='function')return;

  const preview=(title,html)=>`<section style="font-family:system-ui;padding:16px;border:1px solid #334155;border-radius:12px;background:#07111f;color:#e5edf8"><strong style="display:block;margin-bottom:8px;color:#60a5fa">${title}</strong>${html}</section>`;
  const file=(path,detail)=>({path,method:'MANUAL',detail});
  const F=(topic,name,description,code,previewHtml,tip,meta={})=>T(topic,name,description,code,previewHtml,[],{
    kind:'JavaScript',tip,
    guideTitle:'Dónde se hace cada modificación',
    codeLabel:'Código JavaScript',
    filesToCreateTitle:'Archivos que crea el estudiante',
    filesToCreateStatus:'Crea los archivos indicados y usa una API de práctica. No coloques secretos ni API keys privadas en el navegador.',
    ...meta
  });

  sections.push({
    title:'JavaScript · 10B. Fetch API a fondo',
    navLabel:'Fetch API a fondo',
    group:'JavaScript',primaryArea:'JavaScript',course:'JavaScript',areaOrder:195,
    description:'Aprende Fetch API de principio a fin: qué devuelve fetch(), cómo leer Response, GET/POST/PUT/PATCH/DELETE, headers, autenticación, estados de carga, errores HTTP, cancelación y consumo de datos para actualizar el DOM.',
    quote:'“fetch hace la petición; tu código todavía debe validar la respuesta, leer el cuerpo y actualizar la interfaz.”',
    challenge:'Construye un visor de usuarios que muestre loading, error y datos reales desde JSONPlaceholder, con botón para cancelar una petición y un formulario POST de práctica.',
    items:[
      F('fetch','1. Cómo funciona Fetch API','fetch(url, options) inicia una petición HTTP y devuelve una Promise que resuelve a un objeto Response. Que la Promise se resuelva no significa que el servidor haya respondido 200: una respuesta 404 o 500 también puede producir un Response válido. Por eso debes revisar response.ok o response.status antes de procesar el cuerpo.','const response = await fetch("https://jsonplaceholder.typicode.com/users/1");\n\nconsole.log(response.status);\nconsole.log(response.ok);\n\nif (!response.ok) {\n  throw new Error(`HTTP ${response.status}`);\n}\n\nconst user = await response.json();\nconsole.log(user);',preview('Flujo','<p style="margin:0">Acción → fetch() → servidor → Response → response.json() → objeto JavaScript → DOM.</p>'),'Recuerda que fetch y response.json() son operaciones asíncronas distintas.',{
        guide:[['Crear','javascript/fetch-api/app.js','Escribe aquí la petición y revisa status/ok.'],['Abrir','Navegador · DevTools · Network','Comprueba URL, método, status y respuesta.']],
        filesToCreate:[file('javascript/fetch-api/index.html','Interfaz del ejemplo.'),file('javascript/fetch-api/app.js','Lógica Fetch.')]
      }),
      F('async await','2. Fetch con async/await y try/catch','async/await permite leer el flujo de forma secuencial. try/catch captura errores de red y errores que tú mismo lances al detectar un status HTTP no exitoso. finally es útil para apagar un indicador de carga aunque la petición falle.','async function loadUsers() {\n  try {\n    const response = await fetch("https://jsonplaceholder.typicode.com/users");\n    if (!response.ok) throw new Error(`HTTP ${response.status}`);\n    const users = await response.json();\n    return users;\n  } catch (error) {\n    console.error("No se pudieron cargar usuarios", error);\n    throw error;\n  }\n}',preview('Idea clave','<p style="margin:0">await espera cada Promise sin bloquear toda la página; solo pausa esa función async.</p>'),'No uses catch para ocultar errores: informa al usuario y conserva información útil para depuración.',{
        guide:[['Modificar','javascript/fetch-api/app.js','Encapsula cada flujo remoto en una función async.']],filesToCreate:[file('javascript/fetch-api/app.js','Funciones de acceso a API.')]
      }),
      F('response','3. response.json(), text(), blob() y otros cuerpos','Response expone métodos para consumir el cuerpo. response.json() interpreta JSON, text() devuelve texto, blob() sirve para binarios como imágenes y formData() para cuerpos compatibles. El cuerpo normalmente se consume una sola vez, por lo que debes elegir el método según Content-Type.','const response = await fetch("/api/profile");\nconst type = response.headers.get("content-type") || "";\n\nif (type.includes("application/json")) {\n  const data = await response.json();\n  console.log(data);\n} else {\n  const text = await response.text();\n  console.log(text);\n}',preview('Response','<p style="margin:0">Headers indican el formato; después consumes el body con el método adecuado.</p>'),'No llames response.json() por costumbre si el endpoint devuelve texto, archivo o una respuesta vacía.',{
        guide:[['Modificar','javascript/fetch-api/app.js','Lee headers y procesa el cuerpo correcto.']],filesToCreate:[file('javascript/fetch-api/app.js','Lectura de respuestas.')]
      }),
      F('GET','4. GET y parámetros de consulta','GET se usa para obtener datos. Los parámetros se pueden construir con URL y URLSearchParams para evitar concatenaciones frágiles. fetch usa GET por defecto, así que no necesitas declarar method para una consulta simple.','const url = new URL("https://jsonplaceholder.typicode.com/posts");\nurl.search = new URLSearchParams({\n  userId: "1",\n  _limit: "5"\n});\n\nconst response = await fetch(url);\nconst posts = await response.json();\nconsole.log(posts);',preview('GET','<p style="margin:0">GET /posts?userId=1&amp;_limit=5 → lista filtrada.</p>'),'Usa URLSearchParams cuando existan varios filtros, paginación u ordenamiento.',{
        guide:[['Modificar','javascript/fetch-api/app.js','Construye la URL y los parámetros aquí.']],filesToCreate:[file('javascript/fetch-api/app.js','Consultas GET.')]
      }),
      F('POST','5. POST con JSON','Para enviar JSON declara method POST, Content-Type application/json y convierte el objeto con JSON.stringify. El servidor decide si crea el recurso y qué status devuelve; el frontend debe validar response.ok antes de asumir éxito.','const newPost = {\n  title: "Mi práctica",\n  body: "Hola desde Fetch",\n  userId: 1\n};\n\nconst response = await fetch("https://jsonplaceholder.typicode.com/posts", {\n  method: "POST",\n  headers: { "Content-Type": "application/json" },\n  body: JSON.stringify(newPost)\n});\n\nif (!response.ok) throw new Error(`HTTP ${response.status}`);\nconst created = await response.json();\nconsole.log(created);',preview('POST','<p style="margin:0">Objeto JS → JSON.stringify → HTTP body → servidor → respuesta JSON.</p>'),'No envíes JSON sin declarar Content-Type cuando el backend espera application/json.',{
        guide:[['Modificar','javascript/fetch-api/app.js','Crea la función createPost.'],['Modificar','javascript/fetch-api/index.html','Agrega el formulario que produce los datos.']],filesToCreate:[file('javascript/fetch-api/index.html','Formulario POST.'),file('javascript/fetch-api/app.js','Petición POST.')]
      }),
      F('PUT PATCH DELETE','6. PUT, PATCH y DELETE','PUT suele representar reemplazo completo, PATCH modificación parcial y DELETE eliminación. La semántica exacta la define la API; revisa su documentación. No todas las respuestas DELETE traen JSON, así que evita llamar response.json() si el cuerpo está vacío.','await fetch("/api/users/7", {\n  method: "PATCH",\n  headers: { "Content-Type": "application/json" },\n  body: JSON.stringify({ city: "Bogotá" })\n});\n\nconst deleteResponse = await fetch("/api/users/7", { method: "DELETE" });\nif (!deleteResponse.ok) throw new Error(`HTTP ${deleteResponse.status}`);',preview('Métodos','<p style="margin:0">GET lee · POST crea/ordena una acción · PUT reemplaza · PATCH modifica parcialmente · DELETE elimina.</p>'),'No elijas PUT o PATCH por costumbre; sigue el contrato del backend.',{
        guide:[['Modificar','javascript/fetch-api/app.js','Implementa updateUser y deleteUser.']],filesToCreate:[file('javascript/fetch-api/app.js','Métodos HTTP.')]
      }),
      F('headers auth','7. Headers y Authorization','Headers transportan metadatos como Content-Type, Accept o Authorization. Un token Bearer puede viajar en Authorization, pero un secreto privado de servidor nunca debe incrustarse en código frontend porque cualquier usuario puede inspeccionarlo.','const token = sessionStorage.getItem("access_token");\n\nconst response = await fetch("/api/profile", {\n  headers: {\n    "Accept": "application/json",\n    "Authorization": `Bearer ${token}`\n  }\n});',preview('Seguridad','<p style="margin:0">El navegador puede enviar credenciales de usuario; las claves privadas administrativas pertenecen al backend.</p>'),'Distingue un token de sesión del usuario de una API key secreta del servidor.',{
        guide:[['Modificar','javascript/fetch-api/app.js','Agrega headers permitidos.'],['Revisar','Backend / proveedor de API','Confirma CORS y el mecanismo de autenticación.']],filesToCreate:[file('javascript/fetch-api/app.js','Headers de la petición.')]
      }),
      F('UI state','8. Loading, error, vacío y éxito','Una interfaz real necesita estados. Antes de pedir datos muestra Loading; si ocurre error muestra un mensaje; si llega una lista vacía dilo; y solo cuando hay datos renderiza el resultado. Esto evita pantallas silenciosas y mejora accesibilidad.','const output = document.querySelector("#output");\n\nasync function renderUsers() {\n  output.textContent = "Cargando...";\n  try {\n    const response = await fetch("https://jsonplaceholder.typicode.com/users");\n    if (!response.ok) throw new Error(`HTTP ${response.status}`);\n    const users = await response.json();\n    output.innerHTML = users.length\n      ? users.map(user => `<p>${user.name}</p>`).join("")\n      : "<p>No hay resultados</p>";\n  } catch (error) {\n    output.textContent = "No se pudieron cargar los datos";\n  }\n}',preview('Estados de UI','<p style="margin:0">Cargando… → datos / vacío / error.</p>'),'Para datos no confiables prefiere crear nodos y textContent en vez de interpolar directamente innerHTML.',{
        guide:[['Modificar','javascript/fetch-api/index.html','Crea #output y controles.'],['Modificar','javascript/fetch-api/app.js','Gestiona los estados.']],filesToCreate:[file('javascript/fetch-api/index.html','Contenedor de resultado.'),file('javascript/fetch-api/app.js','Estado remoto.')]
      }),
      F('AbortController','9. Cancelar peticiones con AbortController','AbortController permite cancelar una petición que ya no necesitas, por ejemplo cuando el usuario cambia de búsqueda, navega a otra vista o pulsa Cancelar. fetch recibe controller.signal y controller.abort() provoca un AbortError.','const controller = new AbortController();\n\nconst request = fetch("/api/search?q=javascript", {\n  signal: controller.signal\n});\n\ndocument.querySelector("#cancel").addEventListener("click", () => {\n  controller.abort();\n});\n\ntry {\n  await request;\n} catch (error) {\n  if (error.name !== "AbortError") throw error;\n}',preview('Cancelación','<p style="margin:0">Nueva búsqueda → cancela la anterior → evita actualizar UI con datos obsoletos.</p>'),'Cancelar peticiones ayuda a evitar condiciones de carrera en búsquedas rápidas.',{
        guide:[['Modificar','javascript/fetch-api/index.html','Agrega #cancel.'],['Modificar','javascript/fetch-api/app.js','Crea y aborta el controller.']],filesToCreate:[file('javascript/fetch-api/index.html','Botón Cancelar.'),file('javascript/fetch-api/app.js','AbortController.')]
      }),
      F('parallel','10. Varias peticiones con Promise.all','Si varias peticiones son independientes puedes iniciarlas al mismo tiempo y esperar todas con Promise.all. Así reduces el tiempo total frente a esperar una y luego empezar la siguiente. Si alguna falla, Promise.all rechaza; decide si ese comportamiento es el que necesita tu interfaz.','const [usersResponse, postsResponse] = await Promise.all([\n  fetch("https://jsonplaceholder.typicode.com/users"),\n  fetch("https://jsonplaceholder.typicode.com/posts?_limit=5")\n]);\n\nif (!usersResponse.ok || !postsResponse.ok) {\n  throw new Error("Una petición falló");\n}\n\nconst [users, posts] = await Promise.all([\n  usersResponse.json(),\n  postsResponse.json()\n]);',preview('Paralelo','<p style="margin:0">Petición A ↘<br>Petición B → Promise.all → render final<br>Petición C ↗</p>'),'No paralelices operaciones que dependen de la respuesta anterior.',{
        guide:[['Modificar','javascript/fetch-api/app.js','Inicia peticiones independientes juntas.']],filesToCreate:[file('javascript/fetch-api/app.js','Carga paralela.')]
      }),
      F('proyecto','11. Proyecto · visor de usuarios con Fetch','Integra HTML, DOM y Fetch en una práctica. El usuario pulsa Cargar, la interfaz muestra estado, se solicita la API, se comprueba status, se convierte JSON y se crean tarjetas. Añade Cancelar y un botón Reintentar cuando falle.','// estructura sugerida\nfetch-users/\n├── index.html\n├── styles.css\n└── app.js\n\n// app.js\nasync function loadUsers(signal) {\n  const response = await fetch(\n    "https://jsonplaceholder.typicode.com/users",\n    { signal }\n  );\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json();\n}',preview('Resultado esperado','<div style="display:grid;gap:8px"><button>Cargar usuarios</button><div style="padding:10px;border:1px solid #334155;border-radius:8px">Leanne Graham · Bret</div><div style="padding:10px;border:1px solid #334155;border-radius:8px">Ervin Howell · Antonette</div></div>'),'Termina comprobando el flujo también desde Network: request, response, headers, timing y status.',{
        guide:[['Crear','fetch-users/index.html','Botones y región de resultados.'],['Crear','fetch-users/styles.css','Estados loading/error/tarjetas.'],['Crear','fetch-users/app.js','Fetch, AbortController y render.']],filesToCreate:[file('fetch-users/index.html','Estructura.'),file('fetch-users/styles.css','Presentación.'),file('fetch-users/app.js','Lógica completa.')]
      })
    ]
  });
})();
