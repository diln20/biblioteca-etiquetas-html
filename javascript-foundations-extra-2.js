(()=>{
  if(window.__jsFoundationsExtra2)return; window.__jsFoundationsExtra2=true;
  if(!Array.isArray(window.sections)||typeof window.T!=='function')return;
  const L=(...x)=>x.join('\n');
  const F=(path,detail)=>({path,method:'MANUAL',detail});
  const R=(title,text)=>`<section style="font-family:system-ui;padding:16px;border:1px solid rgba(250,204,21,.38);border-radius:12px;background:#0b1220;color:#e5edf8"><strong style="display:block;color:#fde047;margin-bottom:8px">${title}</strong><div style="line-height:1.6">${text}</div></section>`;
  const J=(topic,name,desc,code,out,meta={})=>T(topic,name,desc,code,out,[],{kind:'JavaScript',tip:meta.tip||'Predice el resultado antes de ejecutar y modifica el ejemplo para comprobar la regla.',guide:meta.guide||[['Crear o modificar',meta.path||'javascript/fundamentos/app.js','Escribe aquí el ejemplo.'],['Ejecutar','Navegador · DevTools','Ejecuta y compara el resultado.']],guideTitle:'Dónde practicar y ejecutar',codeLabel:'Código JavaScript',filesToCreate:meta.files||[F(meta.path||'javascript/fundamentos/app.js','Archivo de práctica.')],filesToCreateTitle:'Archivos que se crean en esta lección',exerciseTitle:'Ejercicio para ti',exerciseTasks:meta.tasks||['Ejecuta el ejemplo.','Usa datos diferentes.','Explica el resultado con tus palabras.'],exerciseExtra:meta.extra||'Prueba también un caso límite o inválido.'});
  const A=(title,areaOrder,desc,challenge,items)=>sections.push({title,navLabel:title.replace(/^JavaScript\s*·\s*/,''),description:desc,quote:'“Cuando haces explícitas las conversiones, el código se vuelve más fácil de razonar.”',challenge,group:'JavaScript',primaryArea:'JavaScript',course:'JavaScript',areaOrder,items});

  A('JavaScript · 3A. Conversión, coerción y valores booleanos',35,'Distingue conversión explícita de coerción implícita, aprende truthy/falsy y completa los operadores de asignación y nullish coalescing.','Predice diez expresiones de coerción antes de ejecutarlas y justifica cada respuesta.',[
    J('conversion','1. Conversión explícita','La conversión explícita ocurre cuando decides transformar un valor con Number, String, Boolean, parseInt o parseFloat. Es preferible con datos de inputs o APIs porque hace visible la intención.',L("const cantidad = Number('3');","const entero = parseInt('42px', 10);","const decimal = parseFloat('3.14kg');",'const texto = String(2026);','const activo = Boolean(1);','console.log(cantidad, entero, decimal, texto, activo);'),R('Resultado','3 · 42 · 3.14 · "2026" · true'),{path:'javascript/conversion/conversion-explicita.js'}),
    J('coercion','2. Coerción implícita','La coerción ocurre cuando JavaScript convierte tipos automáticamente. El operador + concatena si aparece un string; -, * y / suelen intentar convertir a number. === compara sin coerción y por eso es la comparación recomendada por defecto.',L("console.log('5' + 2); // '52'","console.log('5' - 2); // 3","console.log('5' * 2); // 10",'console.log(true + 1); // 2',"console.log('' == 0);  // true","console.log('' === 0); // false"),R('Idea clave','+ puede concatenar; otros operadores numéricos fuerzan conversión. === no aplica coerción.'),{path:'javascript/conversion/coercion.js'}),
    J('truthy falsy','3. Truthy y Falsy','En una condición JavaScript convierte el valor a boolean. Son falsy: false, 0, -0, 0n, cadena vacía, null, undefined y NaN. Los arrays y objetos vacíos son truthy.',L('const valores = [false, 0, "", null, undefined, NaN, [], {}];','valores.forEach(valor => console.log(valor, Boolean(valor)));','','console.log(Boolean([])); // true','console.log(Boolean({})); // true'),R('Falsy','false · 0 · -0 · 0n · "" · null · undefined · NaN. [] y {} son truthy.'),{path:'javascript/conversion/truthy-falsy.js'}),
    J('assignment','4. Operadores de asignación','+=, -=, *=, /=, %= y **= combinan una operación con una asignación. Recuerda que += también concatena si el valor actual es string.',L('let puntos = 10;','puntos += 5;','puntos -= 2;','puntos *= 2;','puntos /= 2;','puntos %= 5;','puntos **= 2;','console.log(puntos); // 9'),R('Resultado final','9'),{path:'javascript/operadores/asignacion.js'}),
    J('nullish','5. ??, ||=, &&= y ??=','?? usa el valor de la derecha solo si el izquierdo es null o undefined. || también reemplaza 0, cadena vacía y false. ||=, &&= y ??= realizan asignación condicional siguiendo esas reglas.',L('const cantidad = 0;','console.log(cantidad || 10); // 10','console.log(cantidad ?? 10); // 0','',"let nombre = '';","nombre ||= 'Invitado';",'let tema;',"tema ??= 'oscuro';",'console.log(nombre, tema);'),R('Diferencia','|| trata cualquier falsy como ausencia; ?? solo considera null y undefined.'),{path:'javascript/operadores/logicos-asignacion.js'})
  ]);

  A('JavaScript · 4A. Strings y Numbers a fondo',45,'Aprende índices, longitud e inmutabilidad de strings, sus métodos principales y el manejo numérico con parseo, formato, validación y límites de precisión.','Normaliza un nombre con espacios, calcula un precio decimal y muestra una salida validada.',[
    J('String','1. Strings · índices, length e inmutabilidad','Una cadena es una secuencia de caracteres. Puedes leer por índice y consultar length, pero los strings son inmutables: los métodos devuelven una nueva cadena.',L("const lenguaje = 'JavaScript';",'console.log(lenguaje[0]);      // J','console.log(lenguaje.length);  // 10','', 'const mayuscula = lenguaje.toUpperCase();','console.log(lenguaje);','console.log(mayuscula);'),R('Regla','Los métodos de string no modifican el original; guarda el valor devuelto si quieres conservar la transformación.'),{path:'javascript/strings/basicos.js'}),
    J('String methods','2. toUpperCase, toLowerCase, trim, includes, startsWith y endsWith','Estos métodos sirven para normalizar y buscar texto. trim elimina espacios exteriores; includes busca un fragmento; startsWith y endsWith comprueban los extremos.',L("const entrada = '  JavaScript Moderno  ';",'const texto = entrada.trim();',"console.log(texto.includes('Script'));","console.log(texto.startsWith('Java'));","console.log(texto.endsWith('Moderno'));",'console.log(texto.toLowerCase());'),R('Resultado','true · true · true · javascript moderno'),{path:'javascript/strings/metodos.js'}),
    J('slice replace split','3. slice, substring, replace, replaceAll y split','slice extrae una parte y admite índices negativos. substring extrae rangos, pero trata los negativos de otra forma. replace cambia una coincidencia; replaceAll cambia todas; split devuelve un Array.',L("const ruta = 'curso/javascript/basico';","console.log(ruta.slice(6, 16));       // javascript","console.log(ruta.slice(-6));          // basico",'',"const frase = 'JS es rápido. JS es flexible.';","console.log(frase.replaceAll('JS', 'JavaScript'));","console.log('HTML,CSS,JS'.split(','));"),R('Resultado','Puedes extraer, reemplazar o convertir texto en una lista.'),{path:'javascript/strings/transformaciones.js'}),
    J('Number parse','4. Number, parseInt y parseFloat','Number exige que la cadena completa represente un número. parseInt y parseFloat pueden leer una parte numérica inicial. Elige según el formato real de tus datos.',L("console.log(Number('42'));        // 42","console.log(Number('42px'));      // NaN","console.log(parseInt('42px',10)); // 42","console.log(parseFloat('3.5kg')); // 3.5","console.log(Number(''));          // 0"),R('Consejo','Para inputs numéricos controlados suele ser mejor validar y usar Number.'),{path:'javascript/numeros/conversion.js'}),
    J('Number methods','5. toFixed, isInteger, isNaN e isFinite','toFixed devuelve un string con decimales fijos. Number.isInteger comprueba enteros; Number.isNaN detecta NaN sin coerción; Number.isFinite comprueba que sea un número finito.',L('const precio = 1234.567;','console.log(precio.toFixed(2));          // "1234.57"','console.log(Number.isInteger(10));       // true','console.log(Number.isNaN(NaN));          // true','console.log(Number.isFinite(Infinity));  // false'),R('Importante','toFixed devuelve string, no number.'),{path:'javascript/numeros/metodos.js'}),
    J('precision','6. Precisión y MAX_SAFE_INTEGER','Los Number usan punto flotante IEEE 754, por lo que algunos decimales no se representan exactamente. Para dinero simple es común trabajar en centavos. Para enteros enormes usa BigInt.',L('console.log(0.1 + 0.2); // 0.30000000000000004','console.log(Number.MAX_SAFE_INTEGER);','', 'const precioCentavos = 1999;','const total = precioCentavos * 3;','console.log((total / 100).toFixed(2)); // 59.97'),R('Modelo práctico','No asumas precisión decimal exacta; usa una estrategia acorde al dominio.'),{path:'javascript/numeros/precision.js'})
  ]);

  A('JavaScript · 4B. Objeto Math y aleatoriedad',46,'Completa el manejo numérico con Math: redondeo, máximos, mínimos, potencias, raíces y generación de enteros aleatorios dentro de un rango.','Crea un simulador de dado y demuestra que solo genera valores entre 1 y 6.',[
    J('Math round','1. round, floor, ceil y trunc','round redondea al entero más cercano; floor baja hacia menos infinito; ceil sube hacia más infinito; trunc elimina la parte decimal acercándose a cero.',L('const n = 4.7;','console.log(Math.round(n)); // 5','console.log(Math.floor(n)); // 4','console.log(Math.ceil(n));  // 5','console.log(Math.trunc(n)); // 4','console.log(Math.floor(-4.7)); // -5','console.log(Math.trunc(-4.7)); // -4'),R('Diferencia','floor y trunc se comportan distinto con negativos.'),{path:'javascript/math/redondeo.js'}),
    J('Math min max sqrt pow','2. min, max, abs, sqrt y pow','Math.min y Math.max comparan números; abs devuelve valor absoluto; sqrt calcula raíz cuadrada; pow calcula potencias. El operador ** es una alternativa moderna para potencias.',L('console.log(Math.min(8, 3, 12)); // 3','console.log(Math.max(8, 3, 12)); // 12','console.log(Math.abs(-15));      // 15','console.log(Math.sqrt(81));      // 9','console.log(Math.pow(2, 5));     // 32','console.log(2 ** 5);             // 32'),R('Resultado','3 · 12 · 15 · 9 · 32 · 32'),{path:'javascript/math/operaciones.js'}),
    J('Math.random','3. Math.random · enteros en un rango','Math.random devuelve un valor desde 0 incluido hasta 1 excluido. Para un entero entre min y max inclusive, escala el rango, aplica floor y suma el mínimo.',L('function enteroAleatorio(min, max) {','  return Math.floor(Math.random() * (max - min + 1)) + min;','}','console.log(enteroAleatorio(1, 6));'),R('Rango','Siempre devuelve un entero entre min y max, incluyendo ambos extremos.'),{path:'javascript/math/aleatorio.js'}),
    J('dice','4. Ejercicio · simulador de dado','Combina función, Math.random y Math.floor. Ejecuta muchas veces y comprueba que nunca aparezca 0 ni 7. Math.random no debe usarse para secretos criptográficos.',L('const lanzarDado = () => Math.floor(Math.random() * 6) + 1;','for (let i = 0; i < 10; i += 1) {','  console.log(lanzarDado());','}'),R('Resultado esperado','Diez enteros, todos entre 1 y 6.'),{path:'javascript/math/dado.js',extra:'Amplía el ejercicio para lanzar dos dados y sumar sus resultados.'})
  ]);
})();