(()=>{
  if(window.__cdnFrameworksLibrariesAdded)return;
  window.__cdnFrameworksLibrariesAdded=true;
  if(!Array.isArray(window.sections)||typeof window.T!=='function')return;

  const preview=(title,text)=>`<section style="font-family:system-ui;padding:18px;border:1px solid #38bdf8;border-radius:14px;background:#07111f;color:#e5edf8"><strong style="display:block;color:#67e8f9;margin-bottom:8px">${title}</strong><p style="margin:0;color:#cbd5e1">${text}</p></section>`;
  const guide=(file='index.html',detail='Aquí se enlaza el recurso externo desde el CDN.')=>[
    ['Modificar',file,detail],
    ['Comprobar','DevTools · Network','Recarga la página y confirma que el archivo del CDN responde correctamente, sin 404 ni bloqueos de CSP.']
  ];
  const files=file=>[{path:file,method:'MANUAL',detail:'Crea o reutiliza este archivo para practicar el ejemplo con CDN.'}];
  const C=(topic,name,description,code,previewHtml,tip,file='index.html')=>T(topic,name,description,code,previewHtml,[],{
    kind:'CDN',
    tip,
    guide:guide(file),
    guideTitle:'Dónde se configura el CDN',
    codeLabel:'Código HTML / CDN',
    filesToCreate:files(file),
    filesToCreateTitle:'Archivo para practicar',
    filesToCreateStatus:'Crea el archivo una sola vez y reemplaza únicamente los enlaces CDN de cada ejemplo.'
  });

  sections.push({
    title:'Frameworks frontend · CDN y librerías',
    navLabel:'CDN y librerías',
    group:'Frameworks',
    primaryArea:'Frameworks',
    course:'Frameworks',
    areaOrder:18,
    description:'Aprende cuándo conviene cargar frameworks y librerías directamente desde un CDN, cómo enlazar CSS y JavaScript, cómo usar módulos ES desde el navegador, qué herramientas sí funcionan bien sin build y cuáles necesitan npm/CLI, además de versionado, SRI, CSP y migración a un proyecto con paquetes.',
    quote:'“CDN es una forma de entregar archivos; no reemplaza la arquitectura ni el sistema de paquetes cuando el proyecto necesita compilación.”',
    challenge:'Construye una página de práctica que cargue Bootstrap, Vue y Axios desde CDN, comprueba cada petición en DevTools y después migra al menos una dependencia a npm.',
    items:[
      C('Concepto','1. Qué es un CDN y cuándo usarlo','Un CDN distribuye archivos estáticos desde servidores cercanos al usuario. Para prácticas, prototipos, páginas simples o una librería pequeña puede evitar instalar Node, npm y bundlers. En aplicaciones medianas o grandes conviene usar paquetes locales porque permiten bloquear versiones, compilar, hacer tree-shaking, ejecutar pruebas y controlar mejor el build.','CDN\n  ↓\narchivo CSS / JS externo\n  ↓\nnavegador\n\nCDN = entrega de archivos\nnpm = gestión de dependencias + build',preview('CDN vs npm','Usa CDN para empezar rápido; usa npm cuando el proyecto ya necesita build, pruebas, módulos y control de dependencias.'),'No elijas CDN solo porque requiere menos pasos: primero identifica si la herramienta necesita compilación.'),
      C('Patrón básico','2. CSS con <link> y JavaScript con <script>','Los frameworks CSS se cargan normalmente con link en head. Las librerías JavaScript tradicionales se cargan con script. Si el script no necesita ejecutarse antes del HTML, usa defer para evitar bloquear el parseo. Si el CDN entrega módulos ESM, utiliza type="module".','<!doctype html>\n<html lang="es">\n<head>\n  <meta charset="UTF-8">\n  <meta name="viewport" content="width=device-width, initial-scale=1">\n  <link rel="stylesheet" href="https://cdn.example.com/libreria/1.2.3/libreria.min.css">\n</head>\n<body>\n  <h1>Mi página</h1>\n  <script src="https://cdn.example.com/libreria/1.2.3/libreria.min.js" defer></script>\n</body>\n</html>',preview('Patrón de carga','CSS en head; JavaScript con defer o type="module" según el tipo de distribución.'),'No copies enlaces sin saber si son CSS, UMD, ESM o una build de desarrollo.'),
      C('Bootstrap','3. Bootstrap 5 por CDN','Bootstrap permite usar su CSS y JavaScript compilados sin build. El CSS va en head y bootstrap.bundle incluye Popper para dropdowns, tooltips y popovers. En proyectos React, Vue o Angular evita que el JavaScript de Bootstrap controle el mismo DOM que controla el framework; para muchos casos basta con usar únicamente su CSS.','<!doctype html>\n<html lang="es">\n<head>\n  <meta charset="UTF-8">\n  <meta name="viewport" content="width=device-width, initial-scale=1">\n  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">\n</head>\n<body class="p-4">\n  <button class="btn btn-primary">Guardar</button>\n\n  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>\n</body>\n</html>',preview('Bootstrap por CDN','El navegador descarga bootstrap.min.css y, si lo necesitas, bootstrap.bundle.min.js.'),'En producción copia también integrity y crossorigin desde la documentación oficial de la versión exacta que uses.'),
      C('Tailwind','4. Tailwind Play CDN · solo práctica y desarrollo','Tailwind ofrece Play CDN para probar utilidades directamente en el navegador sin configurar un build. Es excelente para aprender y prototipar, pero su documentación indica que no está pensado para producción. Para un proyecto real usa Vite, CLI o la integración oficial del framework.','<!doctype html>\n<html lang="es">\n<head>\n  <meta charset="UTF-8">\n  <meta name="viewport" content="width=device-width, initial-scale=1">\n  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>\n</head>\n<body class="bg-slate-950 p-8 text-white">\n  <button class="rounded-lg bg-sky-500 px-4 py-2 font-semibold">Guardar</button>\n</body>\n</html>',preview('Tailwind Play CDN','Ideal para ejercicios rápidos; migra al build oficial antes de publicar una aplicación real.'),'No presentes Play CDN como la instalación recomendada de producción.'),
      C('Vue','5. Vue 3 directamente desde CDN','Vue sí soporta oficialmente una ruta sin build. La build global expone Vue en window y la build ESM puede importarse con type="module". Es muy útil para mejorar HTML existente o integrarlo con un backend. La limitación principal es que sin build no utilizas Single-File Components .vue de la forma habitual.','<!doctype html>\n<html lang="es">\n<body>\n  <div id="app">{{ mensaje }}</div>\n\n  <script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>\n  <script>\n    const { createApp } = Vue;\n    createApp({\n      data: () => ({ mensaje: "Hola desde Vue CDN" })\n    }).mount("#app");\n  </script>\n</body>\n</html>',preview('Vue por CDN','No necesitas Vite para este ejemplo; Vue se descarga y monta sobre #app directamente.'),'Para producción sin build usa la distribución .prod.js y fija una versión concreta.'),
      C('Módulos ESM','6. Importar paquetes desde un CDN como módulos','Los navegadores modernos permiten importar módulos ES desde una URL. Algunos CDN transforman paquetes npm a ESM para poder utilizarlos directamente. Esto es útil para pruebas pequeñas; en aplicaciones grandes un bundler controla mejor dependencias transitivas, optimización y compatibilidad.','<!doctype html>\n<html lang="es">\n<body>\n  <p id="id"></p>\n  <script type="module">\n    import { nanoid } from "https://cdn.jsdelivr.net/npm/nanoid@5/+esm";\n    document.querySelector("#id").textContent = nanoid();\n  </script>\n</body>\n</html>',preview('ESM desde CDN','type="module" permite usar import directamente desde una URL HTTPS.'),'Si el proveedor soporta versiones exactas, fíjalas para evitar cambios inesperados.'),
      C('Axios','7. Axios por CDN para consumir una API','Axios puede cargarse como script global en una página sin build. Después aparece el objeto axios y puedes realizar peticiones HTTP. Para proyectos React, Vue, Angular o TypeScript grandes, suele ser preferible instalarlo con npm para que forme parte del grafo de dependencias.','<!doctype html>\n<html lang="es">\n<body>\n  <pre id="salida">Cargando...</pre>\n  <script src="https://cdn.jsdelivr.net/npm/axios@1/dist/axios.min.js"></script>\n  <script>\n    axios.get("https://jsonplaceholder.typicode.com/users/1")\n      .then(({ data }) => {\n        document.querySelector("#salida").textContent = data.name;\n      })\n      .catch(() => {\n        document.querySelector("#salida").textContent = "Error";\n      });\n  </script>\n</body>\n</html>',preview('Axios por CDN','El objeto global axios queda disponible después de cargar el script.'),'Para una práctica está bien fijar la versión mayor; para producción fija la versión exacta.'),
      C('Chart.js','8. Chart.js por CDN','Las librerías visuales también pueden cargarse por CDN. Chart.js ofrece una build UMD que expone Chart globalmente, por lo que basta un canvas y un script. Este patrón es útil en dashboards sencillos o páginas servidas por Django/FastAPI sin pipeline frontend.','<!doctype html>\n<html lang="es">\n<body>\n  <canvas id="ventas"></canvas>\n  <script src="https://cdn.jsdelivr.net/npm/chart.js@4/dist/chart.umd.min.js"></script>\n  <script>\n    new Chart(document.querySelector("#ventas"), {\n      type: "bar",\n      data: {\n        labels: ["HTML", "CSS", "JS"],\n        datasets: [{ label: "Avance", data: [80, 65, 50] }]\n      }\n    });\n  </script>\n</body>\n</html>',preview('Chart.js por CDN','El canvas recibe el gráfico después de que la librería se haya cargado.'),'Carga primero la librería y después el script que la utiliza.'),
      C('Frameworks CSS','9. Bulma y Foundation mediante CDN','Los frameworks CSS sin compilación obligatoria pueden enlazarse desde un CDN como cualquier hoja de estilos. Para prácticas usa una versión mayor; para producción fija la versión exacta y revisa la documentación oficial del proyecto.','<!-- Bulma -->\n<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@1/css/bulma.min.css">\n\n<!-- Foundation -->\n<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/foundation-sites@6/dist/css/foundation.min.css">\n\n<button class="button is-primary">Bulma</button>',preview('CSS por CDN','Un link puede incorporar todo el framework CSS sin npm ni configuración local.'),'No cargues dos frameworks CSS completos en la misma página salvo que estés haciendo una comparación controlada.'),
      C('React, Angular y Svelte','10. Cuándo NO conviene usar un CDN como instalación principal','React puede ejecutarse desde un CDN para una demo pequeña, pero el flujo moderno usa módulos, JSX y build. Angular moderno depende de su toolchain y CLI; no es una buena práctica enseñarlo como un simple script CDN. Svelte compila componentes antes de ejecutarlos. En esos casos CDN sirve mejor para recursos externos concretos que para reemplazar el sistema de build.','React  → demo CDN posible, proyecto real: Vite/framework\nAngular→ CLI + paquetes; no simple <script>\nSvelte → compilación de .svelte\nVue    → sí tiene modo CDN oficial\nCSS    → normalmente fácil por <link>',preview('No todo framework es igual','CDN funciona muy bien con algunas librerías, pero no sustituye el compilador de Angular o Svelte.'),'Enseña siempre la ruta CDN junto a la ruta recomendada para producción.'),
      C('Seguridad','11. Versiones, SRI, crossorigin y CSP','Un CDN es un tercero que entrega código a tu página. En producción fija una versión exacta, utiliza Subresource Integrity cuando el proveedor publica el hash, configura crossorigin correctamente y revisa la Content-Security-Policy. Si una política bloquea cdn.jsdelivr.net o unpkg.com, el navegador no cargará el recurso aunque la URL sea correcta.','<link\n  rel="stylesheet"\n  href="https://cdn.example.com/lib/1.2.3/lib.min.css"\n  integrity="sha384-HASH_PUBLICADO_POR_EL_PROVEEDOR"\n  crossorigin="anonymous"\n>\n\n<!-- CSP ejemplo conceptual -->\nContent-Security-Policy: script-src \'self\' https://cdn.jsdelivr.net;',preview('CDN seguro','Versión exacta + SRI + CSP reducen el riesgo de cambios o recursos no autorizados.'),'Nunca inventes un hash SRI: cópialo de la documentación del archivo y versión exactos.'),
      C('Migración','12. Pasar de CDN a npm/Vite','Una buena progresión didáctica es comenzar con CDN y después migrar la misma página a paquetes. El HTML deja de depender de scripts globales y el código utiliza import. package.json registra versiones y package-lock.json bloquea el árbol resuelto. Después Vite construye los archivos finales que sí pueden distribuirse desde un CDN estático.','npm create vite@latest mi-app -- --template vanilla\ncd mi-app\nnpm install\nnpm install axios\n\n// src/main.js\nimport axios from "axios";\n\naxios.get("/api/datos").then(({ data }) => {\n  console.log(data);\n});\n\nnpm run build',preview('CDN → npm → build','Primero entiendes la librería; después aprendes a gestionarla como dependencia del proyecto.'),'Compara DevTools Network antes y después de compilar para entender qué archivos termina descargando el navegador.')
    ]
  });
})();
